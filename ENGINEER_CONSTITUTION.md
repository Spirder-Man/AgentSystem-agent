# 个人工程终身宪法（永久生效，写入开源仓库根目录 `ENGINEER_CONSTITUTION.md`）

## 序言

本宪法为我终身走向工程天才的底层行为铁律，一切编码、学习、架构、复盘行为均不得违背；所有疑惑、Bug、框架底层、系统异常，均以此两条根本准则作为唯一判断标尺。

## 第一条根本准则：一切工程输出，必须贴合自然人脑认知规律

1. 所有文档、架构图、注释、故障手册、系统血谱、Bug剧本，**第一受众是人**，而非机器、AI、临时交付。
2. 输出要求逻辑线性分层、因果清晰、少晦涩黑盒表述，杜绝堆砌无解释的代码/配置。
3. 架构、故障复盘、底层原理梳理，必须做到：零基础同行通读可完整复现问题、看懂设计取舍、预判同类风险。
4. 拒绝为赶工期生成一次性、无注释、无溯源的快餐代码；任何交付产物都预留人脑可追溯的阅读链路。

### 落地方式（具体操作规范，随实践持续补充）

**方式一：认知线性化——先概念、后全景、再细节**

1. 任何文档的阅读顺序必须符合人脑线性认知：读者在读到任何表格/全景图之前，必须已经认识表中出现的每一个符号（术语、注册方法、机制名）；
2. 禁止"先给表格、后补概念"的倒置结构；表格中的每个陌生符号，必须在该表格之前有定义，或明确指向定义处；
3. 示例：DI 注册表文档把"生命周期/托管服务基础概念"排在注册表之前（docs/learning-notes/2026-08-01_DI注册表_翻译词典目录页.md 第二节）；
4. 读表规则：表格中出现不认识的符号，先回概念节找定义再继续——不许带着半懂往下读。

**方式二：具身类比——工程概念 ↔ 物理生活双向翻译**

1. 每个学到的概念，必须用自己最熟悉的物理经验（运动/生活/身体）翻译一遍，让抽象有根——AI 的类比无根，亲身体验过的抽象才自带"为什么"；
2. 理解天赋（解码他人的抽象）与抽象天赋（编码自己的结构）同等重要，双向翻译才能闭环：概念→生活（外化于行），生活→概念（内化于心）；
3. 翻译用"类比桥三问"：核心机制是什么 → 我的经验里哪段同构 → 哪里失真。知道类比在何处失效，才算真正吃透概念本身；
4. 行动的行不局限于编程——工程经验外化于行，行于球场、行于生活、行于一切决策；篮球等身体实践是工程认知的终身锚点（例：包夹=互斥锁，上篮=瞬时态，控球配合=作用域，单打决策=单例）。

**方式三：零跳跃讲解——概念拆解必须逐层不跨步（AI讲解铁律）**

AI因概率性生成天然存在"概念跳跃"倾向——从A直接跳到C，跳过中间步骤B。这在工程学习中致命：读者大脑中没有B的认知锚点，C就成了空中楼阁。

**反例（跳跃讲解）**：讲解 Subscribe 时直接说"这是事件驱动的解耦"——六个字跳过至少四个中间概念（方法是什么、参数为什么是函数、async/await 为何出现、注册≠执行）。

**正例（零跳跃五步拆解）**——以 Program.cs L400-404 的 Subscribe 讲解为例：

| 步骤 | 拆解内容 | 为什么这一步不能跳 |
|---|---|---|
| Step 1 | `Subscribe` 是什么：EventActionDispatcher 的普通方法，跟任何方法一样，只是参数特殊 | 消解"是不是多态"的误判 |
| Step 2 | `async evt => { ... }` 是什么：lambda 语法——当场定义一个无名函数当参数传进去 | 消解"这是调用还是定义"的模糊 |
| Step 3 | 为什么必须 `await Task.CompletedTask`：类型约束 `Func<PipelineEvent, Task>` 要求返回 Task，只能填一个"什么都不做的 await"应付编译器 | 消解"明明没异步操作为什么要写 await"的困惑 |
| Step 4 | 注册≠执行：这行代码跑完时 `Log.Information` 不会执行，只把函数存进字典；真正执行是 `Publish("FindingCreated")` 被调用时 | 这步跳了，前面三步全白费——读者以为"注册时就在执行" |
| Step 5 | 为什么不用写死下游：Publish 只管喊，Subscribe 只管听——新加动作不改老代码 | 收束到设计价值 |

**反例二（跳跃讲解·数据领域，2026-08-11 实测教训）**：让零数据库概念的学习者直接读 `knowledge_chunks` 表的 14 个字段——跳过"数据库是什么→表是什么→字段是什么→记录是什么→主键/外键是什么"五个前置概念。学习者面对 `embedding vector(768)`、`regulation_type`、`ON DELETE CASCADE` 时大脑没有任何锚点，当场反馈"光看字段谁都得晕"。

**正例（零跳跃五步拆解）**——以数据库表讲解为例：

| 步骤 | 拆解内容 | 为什么这一步不能跳 |
|---|---|---|
| Step 1 | 数据库是什么：装数据的仓库（类比：一个 Excel 文件 / 档案室） | 消解"数据库是不是某个软件"的模糊 |
| Step 2 | 表是什么：仓库里的一张工作表（类比：Excel 里的一个 sheet / 档案柜） | 消解"表和库什么关系"——库是容器，表是容器里的分格 |
| Step 3 | 字段是什么：工作表的表头列（姓名/年龄），决定每行能记什么 | 消解"字段是干嘛的"——字段定义"记什么"，行定义"记了谁" |
| Step 4 | 记录/行是什么：一行具体数据（张三，25岁） | 消解"行和列谁先谁后"——先有字段定义，才能填行 |
| Step 5 | 主键/外键是什么：行的身份证号 / 行与行之间的"参见"标注 | 消解"表之间怎么关联"——外键就是一条标注：本行参见某表的某行 |

**铁律**：
1. 每次拆解概念，先问自己"读者在读到上一句话时，脑子里有没有这句话需要的所有前置概念？"——没有的话必须补，不能假定读者自动补全；
2. 禁止用六个字概括六个概念——每个概念单独一步，每步讲完确认读者跟上再进下一步；
3. 如遇新概念，当场拆到不能再拆为止——"lambda"拆到"匿名函数"，"匿名函数"拆到"把函数当参数传"；
4. 本规则的检验标准：读者能否用一句话讲出刚才每个 Step 的结论。如有一步答不上来 → 那步跳跃了，重讲；
5. 顺序铁律：讲解任何"字段级/代码级"细节之前，必须先确认读者认识基础符号（表/行/字段、函数/参数/调用）；读者连"这张表/这个类是干嘛的"都不知道时，禁止进入细节；
6. 顺序铁律：先讲"它解决什么问题（用途故事）"→ 再讲"细节怎么支撑这个用途"——用途是 B，细节是 C，B 不立，C 必塌。

## 第二条根本准则：遇一切似懂非懂的底层机制，必须完整吃透根源

### 总则

在工程领域，**似懂非懂是最大的认知陷阱，彻底溯源是终身追求的乐趣与立身之本**。任何框架行为、并发机制、DI生命周期、LLM管线、存储约束、网络交互、系统底层异常，只要存在模糊、反直觉、只知修复不知成因，必须启动标准化深挖流程，绝不浅尝辄止、止步于复制解决方案。

### 强制执行流程（三条进阶习惯固化为法定流程，缺一不可）

#### 流程1：最小自研复现常态化（底层吃透核心手段）

1. 只要产生疑惑，脱离业务项目、脱离AI生成，独立编写**最小可运行Demo**复现完整机制：
   - DI/并发锁/缓存/异步死锁/数据库事务/RAG检索/审计哈希链/控制器生命周期等全部适用；
2. Demo必须包含正反两套用例：错误写法（复现故障）、标准规范写法（根治方案）；
3. 不允许仅靠AI口述原理，必须亲手编码、打印日志、观测对象引用、复现异常现象，亲眼验证根因；
4. 实验完成后归档源码至开源仓库 `docs/learning-notes/lab-demos/`，永久留存。

#### 流程2：建立专属反直觉Bug知识库（风险感知长效沉淀）

1. 所有自研实验、线上真实故障中**违背直觉、极易踩坑**的底层陷阱，统一录入仓库病灶登记表（系统血谱配套）；
2. 每条记录固定四要素：现象、根因底层机制、错误示例、标准规避模板；

示例归档条目：
- Controller默认为瞬时态，内部new SemaphoreSlim会导致全局并发限流完全失效；
- SemaphoreSlim不可重入，漏写finally.Release会引发锁永久耗尽、服务假死；
- 单进程信号量在多实例Docker部署下完全失去互斥效果，需改用分布式锁；

3. 每次架构设计、代码评审前主动查阅知识库，提前规避同类底层缺陷。

#### 流程3：开源仓库作为终身能力试验田，所有学习成果闭环沉淀

1. `agent-system` 不只是化工AI业务产品，更是个人工程认知唯一永久载体；
2. 每一次底层自研Demo、架构推演、故障复盘、原理手册，全部纳入仓库标准化文档目录；
3. 业务项目踩坑仅为输入，开源沉淀为永久输出，形成「业务遇坑→底层自研吃透→归档沉淀→反哺业务架构」正向闭环；
4. 拒绝碎片化笔记、本地零散代码，一切有价值的底层认知必须纳入版本控制，终身可查阅、可迭代、可展示。

### 准则精神注解

我不以"修复Bug、交付需求"为终点，而以**看透抽象之下全部底层运转逻辑**为终身追求。

他人遇到底层疑惑选择跳过、复制AI代码、临时凑活交付；我将刨根溯源、亲手复现、完整沉淀作为刻入习惯的本能。

抽象层总有失效的一刻，唯有吃透底层的人，才能在凌晨故障、复杂架构、资源瓶颈面前拥有绝对掌控力，这是工程天才与普通开发者的核心分界线。

## 第三条根本准则：一切学习与验证，必须基于真实数据落地（数据落实铁律，2026-08-11 确立）

### 总则

学习任何设计（表结构/字段/接口/配置/算法），必须对照**真实运行数据**观察——**数据是设计的唯一实证**。把"数据"和"字段设计"分开看是纸上谈兵：字段只告诉你"设计者打算记什么"，真实数据才告诉你"实际记了什么、为什么这样记"。没有真实数据的学习等于对着图纸想象实物，一切结论都可能是错的。

### 强制铁律

1. 学习/验证任何设计，必须先获取对应**真实数据**（本地库有则用本地的，本地无则从真实运行环境拉取，禁止凭空想象数据长什么样）；
2. **一切数据禁止伪造**——演示、验证、学习材料必须来自真实产生或真实采集的数据；用伪造数据得出的结论等于没有验证，且会污染判断力；
3. 数据与设计必须同屏对照：先看设计（字段/表结构），再看真实数据（具体行），互为印证——脱离数据的结构讲解和脱离结构的数据堆积都是残缺学习；
4. 拉取真实数据遵循最小化与脱敏：仅拉取学习所需表与字段，敏感凭据与隐私字段不入文档；
5. 验证流程固定为：设计讲解 → 真实数据对照 → 结论归档；归档结论必须标注数据来源与采集时间；
6. **数据留存验证铁律（测试第六维度，2026-08-11 疑案教训）**：任何端到端/集成测试只断言"API 返回正确"不构成数据验证——必须额外以**独立连接**查询数据库，断言写入的行数 > 0；禁止以内存缓存、Stub、同连接可见性作为留存证据。背景：2026-07-30 E2E 评测写入的数据在 8-3 疑案中引发“丢失恐慌”（后经 L0 实测证实数据完好，见第四条根本准则），暴露无留存断言时异常难以被发现、易触发误判链；
7. **破坏性数据库操作铁律**：任何 DROP/TRUNCATE/全表 DELETE/init_database 重建，执行前必须 pg_dump 备份并留存审批记录；测试流程、部署脚本中禁止包含任何清库步骤；数据库服务必须持久化（数据目录位于持久盘或定时备份），禁止依赖易失存储——AutoDL 系统盘存在断电/实例重置丢失风险（2026-08-11 实测断电后 crash recovery 数据完好，但风险真实存在，必须常态化备份）。

## 第四条根本准则：物理事实优先——事故调查必须先验证本体，禁止叙事先行（2026-08-11 误判事件确立）

### 背景（仅记录验证过的事实）

2026-08-11 数据丢失疑案：因 sessions 表 0 行（僵尸表，程序从不写入）触发"数据被清库"误判链——checkpoint 日志被解读为表重建、WAL 18KB 被外推为 DROP 15 张表、排除法推定"外部手动 SQL"，误判结论一度写入本宪法与速查手册。最终 WAL 挖掘 + 直接查库证实：数据从未丢失（audit_logs 1189 行、refresh_tokens 427 行、knowledge_chunks 55 行全部完好），8-3 仅 2 条登录 INSERT，无任何 DROP/TRUNCATE。误判根因：整个调查未执行一次 SELECT count(*)，结论全部建立在间接证据的解读上，叙事先于验证。

### 证据金字塔（从底到顶，结论必须锚定在已获取的最高级别）

- **L0 物理事实**：直接查库（行数/时间范围/表文件存在性）、进程状态、文件系统实测 → 唯一可下结论的级别
- **L1 强间接证据**：WAL 内容（pg_waldump）、审计/DDL 日志 → 可解释因果
- **L2 弱间接证据**：checkpoint、mtime、文件大小、日志数量级 → 只能提出假设
- **L3 排除法**：代码/脚本/提交排查 → 只能排除嫌疑，不能指认凶手

### 强制铁律

1. 任何"数据丢失/损坏/被删"的怀疑，第一动作必须是 L0 验证（SELECT count(*) + 时间范围 + 表文件），禁止在 L0 完成前构建因果叙事；
2. 调查顺序固定 L0 → L1 → L2 → L3，禁止倒序、禁止跳级、禁止以 L2/L3 证据下 L0 结论；
3. 一切结论必须标注证据级别；L2 及以下的表述只能写作"假设（待验证）"，禁止写入结案报告等持久化文档；
4. 排除法只能排除不能指认——"不是 A/B/C 所以是 D"是逻辑跳跃，禁止使用；
5. 叙事检查：结案报告出现"凶手/清库/重建/删除"等定性词，必须附 L0/L1 直接证据；无证据的定性词一律删除；
6. 反转自检：结论被新证据推翻时，必须完成认知链复盘——从哪一步开始从"验证"滑向"叙事"，并修正所有已污染文档（宪法、手册、记忆、知识库）；
7. 疑案闭环四件套：每起事故调查完成必须输出——① 事实报告（L0 证据表，附采集时间）；② 认知复盘（误判链分析）；③ 宪法修订（新教训条款，背景只写验证过的事实）；④ 污染修正清单（被错误结论影响的文档及更正记录）。

## 补充附则（终身约束，辅助根本准则落地）

1. AI仅作为辅助工具，**底层原理推演、架构边界划分、故障根因定位、自研Demo编码**必须独立思考完成，禁止全盘交付AI包办；
2. 任何技术问题仅临时修复不溯源、不归档、不复现底层机制，视为违反第二条根本准则；
3. 所有文档、图纸、代码注释若晦涩难懂、违背人脑阅读逻辑，视为违反第一条根本准则，必须重构；
4. 本宪法永久有效，随技术认知迭代可补充附则，但根本准则永不修改、永不废除；
5. 任何学习、验证、演示行为缺少真实数据支撑，或使用伪造数据，视为违反第三条根本准则，必须立即补上真实数据对照；
6. 任何数据丢失/清库疑案，必须按第四条根本准则完成 L0 验证与根因追溯后才算闭环（2026-08-11 疑案：先经误判链定性"外部清库"，后 WAL 挖掘与直接查库证实数据完好，误判根因=叙事先于验证、全程未做 L0 直接查库）；
7. 任何调查结论写入持久化文档（宪法/手册/记忆/知识库）前，必须标注证据级别（L0 物理事实 / L1 强间接 / L2 弱间接 / L3 排除法）；L2 及以下只能作为"假设（待验证）"记录，禁止以结论形态入库；
8. 发现已入库的错误结论（误判），必须 24 小时内完成全链路污染修正：溯源该结论影响过的所有文档与记忆，逐一更正并留存更正记录。
9. **异地备份铁律（2026-08-11 确立）**：数据库备份必须异地留档——除服务器本地（base + WAL 归档 + pg_dump + cron）外，备份必须定期拉到本地/另一台机器，防止服务器整体不可用（宕机/实例重置/租约到期）时无备份可恢复；异地留档后必须实测恢复一次（pg_restore 到临时库验证行数），"能备份"不等于"能恢复"。
