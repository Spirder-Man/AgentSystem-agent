# 故障排查与修复文档

> **目的**：按 error-maintainer 标准格式记录每次 Bug 修复，形成可追溯的故障清单。
> **文档版本**：v1.1 | **创建日期**：2026-07-02 | **最后更新**：2026-07-03

---

## 错误概览

| 指标 | 数值 |
|------|------|
| 总错误数 | 3 |
| 已修复数 | 3 |
| 未修复数 | 0 |
| 最近更新时间 | 2026-07-03 |

---

## 错误记录

| 错误ID | 场景 | 现象 | 位置 | 根本原因 | 修复方案 | 出现次数 | 状态 | 修复时间 |
|--------|------|------|------|---------|---------|---------|------|---------|
| E017 | T13 评测集全量执行 | 63 条用例全部 FormatException，tool_call_rate=0 | AgentDialog.cs:519 | C# 字符串插值中 `{Guid.NewGuid():N[..6]}` 的 `:` 是格式分隔符，冒号后 `N[..6]` 被整体当作 Guid.ToString() 的格式参数，而非 `.ToString("N")` + `[..6]` 切片 | 将 `.ToString("N")` 和 `[..6]` 移到插值表达式 `{}` 外部：`Guid.NewGuid().ToString("N")[..6]` | ⭐ | 已修复 | 2026-07-01 |
| E019 | T13 心跳监控（F005 HyDE 路径） | 静默卡死：进程存活(PID 2929)但不产出结论，CheckStorageCompatibility → HyDE → CheckStorageCompatibility 无限递归，单 case 超 1800+ 行日志 | LlmService.cs:124（FC 硬编码）、ILlmService.cs、HybridKnowledgeBaseService.cs、RunReflectionStreamTools.cs | InvokeStreamAsync 硬编码 `FunctionChoiceBehavior.Required()`，HyDE 提示词被强制调用工具 → 触发 CheckStorageCompatibility → 递归。Bug-018 是显性死循环会超时，Bug-019 是**静默卡死**（进程永远不死、监控不告警） | FC 策略参数化架构重构：ILlmService/LlmService 新增 `FunctionChoiceBehavior?` 参数（默认 Required 向后兼容），HyDE→None() 阻断递归，Reflection 分层（Step1→Auto, Step4/5→None） | ⭐ | 已修复 | 2026-07-03 |
| E018 | T13 业务评测（F005 综合审核） | 评测卡死，CheckStorageCompatibility 被调用 40+ 次，LLM 持续输出不终止 | LlmService.cs:615 | `FunctionChoiceBehavior.Required()` 每轮 HTTP 请求强制调用工具，工具返回后 SK 内部自动继续下一轮 → Required() 再次生效 → 无限循环 | 业务评测路径改用 `FunctionChoiceBehavior.Auto()`，LLM 自主决定是否调用工具；Required() 仅保留在 FC 就绪性检查 | ⭐ | 已修复 | 2026-07-02 |

---

## 高频错误分析

> 当前 3 条记录，E018/E019 同属 FC 配置族（Required 策略误用），需关注 FC 策略参数化是否全面覆盖所有调用点。后续若出现同类别复发，在此补充预防建议。

---

## 标准错误分类

| 类别 | 说明 | 本次涉及 |
|------|------|:---:|
| 语法/格式错误 | 编译器解析规则导致的运行时异常 | E017 |
| 配置/策略错误 | 框架参数选型不符合场景需求 | E018, E019 |
| API 设计缺陷 | 接口缺少场景化参数导致调用方无法控制行为 | E019 |
