# Agent1 Bug 知识库

> **目的**：结构化记录每个 Bug 的根因/修复/影响模块，实现跨批次知识累积，避免同类问题反复出现。
> **使用方式**：每次修复 Bug 后按模板录入；每次分析日志前浏览「系统弱点了然表」确定重点监控项。
> **文档版本**：v2.0 | **创建日期**：2026-06-30 | **最后更新**：2026-07-03（新增 Bug-026/027/028：评测集GB数据错误 + Citation Accuracy误判 + 版本幻觉三层约束）

---

## 系统弱点了然表（每次测试前必查）

> 这些是已知的系统性弱点，测试时应重点监控。

| # | 弱点 | 已知表现 | 监控信号（grep 关键词） | 关联 Bug |
|:---:|------|------|------|:---:|
| W1 | **LLM 流式输出失控** | 死循环、重复输出、括号级联 | `⚠️ [截断]` / `生成错误` / 单日志行数>500 | [Bug-011](#bug-011) [Bug-012](#bug-012) |
| W2 | **GB 标准编号幻觉** | Qwen3:8b 输出错误 GB 子编号 | `Citation Accuracy` < 70% / `✗ GB` | [Bug-013](#bug-013) |
| W3 | **UTF-8 编码一致性** | Windows GB2312 → 中文乱码(锟斤拷) | `.log` 文件前3行含乱码字符 | [Bug-014](#bug-014) |
| W4 | **FC 工具调用跳过** | LLM 绕过工具直接编造答案 | `[SK诊断] 本轮共调用 0 个工具` | [Bug-001](#bug-001) [Bug-015](#bug-015) |
| W5 | **Embedding 服务稳定性** | 501 / 模型加载超时 | `向量请求失败` / `NotImplemented` | — |
| W6 | **数据库事务一致性** | tx 参数重复 / 并发冲突 | `NpgsqlException` / SQLite 锁 | [Bug-009](#bug-009) |
| W7 | **菜单导航路径漂移** | 菜单结构变动后脚本输入失配 | 交互死锁 / 超时（多个测试同时） | — |
| W8 | **评测格式匹配脆弱** | 模型输出格式变化导致评分归零 | `Faithfulness=0` / `格式不匹配` | [Bug-010](#bug-010) |
| W9 | **KV Cache 跨请求累积（LCP slot 复用）** | FC 退化、重试死亡螺旋、重复输出 | `LLM 调用耗时 >100s` / `生成错误: ...canceled` / 单 case 重复输出 >30 行 / llama-server slot 复用日志 | [Bug-015](#bug-015) [Bug-016](#bug-016) |
| W10 | **FC Required() 策略误用于业务评测** | 死循环、同一工具被调用 40+ 次、评测卡死 | `[工具诊断]` 同一工具名连续出现 >5 次 / 单 case 耗时 >120s | [Bug-018](#bug-018) |
| W11 | **HyDE + FC 递归死循环（静默卡死）** | 进程存活但无输出，FC 重试环路不崩溃 | `[SK诊断]` FC 交替出现 Required/None / 单 case 日志行数 >1800 / 同一工具名交替出现 | [Bug-019](#bug-019) |
| W12 | **评测框架工具白名单与用例不同步** | 新增用例类别工具触发率为 0 | `isInfoQuery` 白名单工具数变化 / G/H/I 类 tool_call_rate=0 | [Bug-020](#bug-020) |
| W13 | **双通道保护多路径不一致** | 对话路径有完整保护但评测路径缺失，新增路径默认不继承保护机制 | `DecoupledPipeline` 日志 0 次 / 对话vs评测路径指标差异 >10% | [Bug-023](#bug-023) |
| W14 | **结论提取器数据源误匹配** | 提取的 GB 编号不出现在 LLM 输出文本中，导致危险类别查询结论虚高失败 | `⚠️ 结论: reg=` 与 LLM 输出原文不一致 / A 类结论准确率异常低 | [Bug-024](#bug-024) |
| W15 | **Answer Relevance 评估器 FC 重复输出** | 单 case 产生 66+ 条相同评分文本，触发流式截断 | `⚠️ [截断]` + 连续 30+ 行相同评分文本 / `FC=RequiredFunctionChoiceBehavior` | [Bug-025](#bug-025) |
| W16 | **评测集 expected_regulation_number 与数据库不同步** | 评测人手动填写GB编号可能与ChemicalSubstanceDatabase实际映射不一致 | `Level4 幻觉扣除` / 危险类别结论准确率异常低（<50%） | [Bug-026](#bug-026) |
| W17 | **Citation Accuracy 未区分数据源** | RAG文档检索不到的值被误判为幻觉，实际来源是C#硬编码字典 | `Citation Accuracy` = 0% / 大量 `🔍 Level4 KB验证: ✗` | [Bug-027](#bug-027) |
| W18 | **LLM 自推断法规版本年份** | Qwen3:8b 在无工具调用时自行编造年代版本号（如 GB 15603-2023） | 回答含 `GB XXXX-YYYY` 但YYYY与数据库不一致 / `🔧 版本校正` | [Bug-028](#bug-028) |

---

## Bug 目录（按时间倒序）

| ID | 日期 | 等级 | 类别 | 标题 |
|:---:|------|:---:|------|------|
| [Bug-019](#bug-019) | 07-03 | P0 | LLM配置 | HyDE + FunctionChoiceBehavior.Required() 导致 FC 递归死循环（F005 静默卡死） |
| [Bug-026](#bug-026) | 07-03 | P0 | 评测数据 | 评测集7条危险类别expected_regulation_number与数据库不对齐（结论准确率仅40%） |
| [Bug-027](#bug-027) | 07-03 | P0 | 评测引擎 | Citation Accuracy 误判数据库源GB编号为幻觉（42条中仅8-12条为真实幻觉） |
| [Bug-028](#bug-028) | 07-03 | P1 | LLM幻觉 | Qwen3:8b 自行编造法规年代版本号（GB 15603-2023实际为2022）+ 三层约束修复 |
| [Bug-018](#bug-018) | 07-02 | P0 | LLM配置 | FunctionChoiceBehavior.Required() 导致评测死循环 |
| [Bug-024](#bug-024) | 07-03 | P1 | 评测 | 结论提取器数据源误匹配 — A 类15条中9条结论虚报⚠️，LLM实际输出正确 |
| [Bug-025](#bug-025) | 07-03 | P1 | 评测 | Answer Relevance 评估器 FC Required() 导致重复输出+流式截断（I001 66+条重复） |
| [Bug-022](#bug-022) | 07-03 | P2 | 评测 | 忠实度 + Citation Accuracy 无审计追踪（56.6%/45.8% 不可追溯） |
| [Bug-023](#bug-023) | 07-03 | P1 | 评测 | 评测路径双通道不完整 — 缺 FactAssembler+兜底导致忠实度未升反降 |
| [Bug-021](#bug-021) | 07-03 | P1 | 评测 | C 类结论判定过松 — Level 1-3 缺少 Level 4 反向幻觉扣除（准确率仅 33%） |
| [Bug-020](#bug-020) | 07-03 | P0 | 评测 | info_query 工具白名单不足 — G/H/I 三类 13 条全挂（工具触发率 0） |
| [Bug-017](#bug-017) | 07-01 | P0 | C#语法 | 字符串插值 Guid 格式说明符歧义导致全量 FormatException |
| [Bug-015](#bug-015) | 06-30 | P0 | LLM配置 | KV Cache 溢出导致 FC 退化（C008 重现） |
| [Bug-016](#bug-016) | 06-30 | P0 | 重试螺旋 | KV Cache 不足 + 重试反馈环 → 评测死亡循环（C015） |
| [Bug-014](#bug-014) | 06-30 | P0 | 编码 | UTF-8 控制台编码三层修复（锟斤拷乱码） |
| [Bug-013](#bug-013) | 06-30 | P1 | LLM幻觉 | Qwen3:8b GB 标准编号幻觉 |
| [Bug-012](#bug-012) | 06-30 | P0 | 流式输出 | T6 RAG 数据校验步骤死循环 |
| [Bug-011](#bug-011) | 06-30 | P0 | 流式输出 | T5 Reflection 自纠错死循环 |
| [Bug-010](#bug-010) | 06-29 | P1 | 评测 | T13 评分性能+忠实度格式匹配双修复 |
| [Bug-009](#bug-009) | 06-29 | P0 | 数据库 | InsertSubstance 首条记录 tx 参数重复 |
| [Bug-008](#bug-008) | 06-29 | P0 | 数据库 | 数据库事务未正确提交（P0-7） |
| [Bug-007](#bug-007) | 06-16 | P0 | 工具函数 | 正则表达式 `>=s*` 安全距离提取失效 |
| [Bug-006](#bug-006) | 06-16 | P0 | 并发 | Timer(async void) 并发同步冲突 |
| [Bug-005](#bug-005) | 06-16 | P1 | 检索 | BM25 `_avgDocLength` 除零 |
| [Bug-004](#bug-004) | 06-16 | P1 | 检索 | ReflectionVerifier 检索异常错误标记 |
| [Bug-003](#bug-003) | 06-16 | P2 | 评测 | ConclusionVerifier 双格式匹配缺失 |
| [Bug-002](#bug-002) | 06-16 | P0 | 缓存 | RagCache 并发安全问题 |
| [Bug-001](#bug-001) | 06-16 | P0 | 检索 | RRF 去重键非确定性 |

---

## Bug 详情

### Bug-015：KV Cache 溢出导致 FC 退化（C008 重现）{#bug-015}

| 字段 | 内容 |
|------|------|
| **发现日期** | 2026-06-30 |
| **严重等级** | 🔴 P0 — 评测集后半段全部失效 |
| **发现场景** | T13 合规评测集重跑 — C008 时 LLM 绕过 Function Calling，重复输出同一答案 90+ 次（83.9s，4155 tokens），后续 C010-C014 持续恶化 |
| **影响模块** | `scripts/zh-diag.sh` L228, `Agent1/Services/AI/LlmService.cs` L117-L126 |
| **根因** | **真正机制：LCP（最长公共前缀）slot 复用导致 KV Cache 跨请求累积**。llama-server 默认 `-sps 0.5`（50% 前缀匹配即复用 slot）+ `cache_prompt: true`（默认缓存 prompt KV）。63 条评测 case 共享相同 system prompt 前缀 → 被分配到同一个 slot → slot 的 KV Cache 跨请求累积（case1 + case2 + ... + case63）→ 总 token 超过 `-c 8192` → llama.cpp 截断最早的 tokens（含 system prompt 中的 FC 格式说明）→ 模型无法生成正确的 FC JSON → SK 的 `FunctionChoiceBehavior.Required()` 一直等待 → 2min 超时 → 模型开始自由输出文本（重复循环）。**证据来源**：llama.cpp GitHub Discussion #13606。注意：`-c 8192` 仅是触发条件，真正的架构缺陷是服务端的 slot 复用机制，不是单次请求太大 |
| **修复** | **三层防御架构（T13 无状态架构）**：① **服务端**：`zh-diag.sh` 增加 `-sps 0.0`（禁用 LCP slot 匹配，每个请求独立 KV Cache）+ `-c 32768`（4 倍扩容）+ `--cache-type-k q8_0 --cache-type-v q8_0`（KV Cache 量化）+ `-fa`（Flash Attention）；② **客户端**：新增 `TokenBudgetManager.cs`（Token 预算检查 + Prompt 裁剪）；③ **评测层**：`EvalEngine` 按意图裁剪工具集（info_query=3 工具、compliance=5 工具）+ `LlmService` 注入 `cache_prompt: false` + AgentDialog 每次创建独立 Session |
| **修复提交** | 本次 |
| **关联 Bug** | [Bug-016](#bug-016) 同一根因不同表现 |
| **验证方法** | T13 重跑全 63 条完成无中断；`grep "未调用任何工具" T13*.log` 无新增匹配 |
| **教训** | llama.cpp `-c` 参数必须 ≥ 最大 prompt 长度（system + tools + user + tool_results）× 安全系数 1.5 = 约 15000-20000 tokens，默认 8192 仅适合简单对话。Q4_K_M 量化模型在 KV Cache 不足时 Function Calling 能力急剧衰减 |
| **追问深度** | 6 层（现象 → FC 退化 → KV Cache 机制 → LCP slot 复用 → GPU 显存分配 → -ngl 参数设计意图） |

## 思维链路（对话复盘）

> 记录本次 Bug 分析的关键思考节点，使后续复盘时不依赖记忆即可还原推理过程。

| # | 节点类型 | 触发问题 / AI 追问 | 关键发现 | 决策 | 否决的路径 |
|:---:|------|------|------|------|------|
| N1 | 现象确认 | T13 评测 C008 开始 FC 退化，LLM 绕过工具直接编造答案，重复 90+ 次 | FC 退化不是随机偶发——从 C008 开始有规律地恶化 | 进入系统性根因分析，不满足于"偶发"解释 | — |
| N2 | L1→L2 追问 | 为什么 FC 格式会丢失？KV Cache 不足的后果是什么？ | KV Cache 不足 → 截断最早 tokens → system prompt 中 FC 格式说明被裁掉 → 模型无法生成 FC JSON | 从 llama.cpp 文档和社区讨论验证假设 | 怀疑是模型量化精度问题——被"C008 开始恶化"的模式排除 |
| N3 | L2→L3 追问 | 为什么 slot 会被跨请求污染？llama.cpp 的 slot 管理机制是怎样的？ | llama-server `-sps 0.5` 默认 LCP slot 复用 → 63 case 共享相同 slot → KV Cache 累积 | 调研 GitHub Discussion #13606 确认 `-sps` 机制，找到证据来源 | 仅在客户端做 prompt 截断——治标不治本 |
| N4 | 方案分支 | 服务端三层防御 vs 客户端单层防御？ | 服务端 `-sps 0.0` 从根源解决 slot 污染；客户端 TokenBudgetManager 做预算保护；评测层 Per-Case 裁剪降低单次负载 | 选三层防御：互相兜底，任何一层故障另两层仍可保护 | 仅调大 -c 参数：延迟问题复发，不能阻止累积 |
| N5 | 横向对比 | 参考 Qoder/Cursor/豆包 如何处理长上下文？ | 业界最佳实践：隔离 + 预算 + 动态裁剪 | 将外部调研的工程实践融合进 T13 无状态架构 | — |

---

### Bug-016：KV Cache 不足 + 重试反馈环 → 评测死亡循环（C015）{#bug-016}

| 字段 | 内容 |
|------|------|
| **发现日期** | 2026-06-30 |
| **严重等级** | 🔴 P0 — C015 永久卡死，评测无法完成 |
| **发现场景** | T13 合规评测集重跑 — C015「苯乙烯+过氧化物储存兼容性」陷入死循环：LLM 调用 1012s→1272s→2249s→2427s，每次超时后 SK 重试将前次工具结果注入 Prompt → 上下文进一步膨胀 → 越重试越慢 |
| **影响模块** | `scripts/zh-diag.sh` L228, `Agent1/Services/AI/LlmService.cs` L110（2min 超时）, L358-L402（重试机制）, `Agent1/Services/Dialog/AgentDialog.cs` L425-L453（EvalFast 调用链） |
| **根因** | **LCP slot 复用导致 KV Cache 跨请求累积 + 重试反馈环**：① llama-server `-sps 0.5` 默认 slot 复用 → 63 case 的 KV Cache 累积在同一 slot → 超出 `-c 8192` → 模型无法在受限上下文中生成 FC JSON → 超时；② SK 重试时 `FunctionCallDiagnosticsFilter` 将前次成功的工具调用结果注入 Prompt（`已保存 N 个工具结果供重试用`）→ Prompt 更大 → 更容易超时 → 再次重试 → Prompt 进一步膨胀（日志中从 1 个工具结果增长到 3 个，RAG 检索结果从 3 条增长到包含完整法规全文）→ 最终 LLM 调用耗时达 2427s 仍失败 |
| **修复** | 同 Bug-015 — 三层防御架构（`-sps 0.0` 禁用 slot 复用 + TokenBudgetManager + Per-Case 工具裁剪 + `cache_prompt: false`）从根源阻断反馈环 |
| **修复提交** | 本次 |
| **关联 Bug** | [Bug-015](#bug-015) 同一根因，[Bug-011](#bug-011) [Bug-012](#bug-012) 同属 LLM 输出质量族 |
| **验证方法** | C015 苯乙烯+过氧化物查询正常完成，单 case 耗时 < 60s；日志中无 `生成错误: ...canceled` 或 `保存 N 个工具结果供重试用` |
| **教训** | 重试机制在资源不足场景下可能成为"毒药"：每次重试增加上下文重量反而降低成功率。必须从根因（上下文容量）修复，不能仅靠重试。熔断器应检测"重试成功率下降"信号并提前中断 |
| **追问深度** | 5 层（现象 → 重试反馈环 → slot 复用根因 → Prompt 膨胀机制 → 重试毒药效应） |

## 思维链路（对话复盘）

> 记录本次 Bug 分析的关键思考节点，使后续复盘时不依赖记忆即可还原推理过程。

| # | 节点类型 | 触发问题 / AI 追问 | 关键发现 | 决策 | 否决的路径 |
|:---:|------|------|------|------|------|
| N1 | 现象确认 | T13 C015 苯乙烯+过氧化物永久卡死，LLM 耗时 1012s→1272s→2249s→2427s | 越重试越慢——耗时呈指数增长，不是线性 | 确认是正反馈恶化的重试螺旋 | — |
| N2 | L1→L2 追问 | 为什么越重试越慢？SK 重试时做了什么？ | SK `FunctionCallDiagnosticsFilter` 将前次工具结果注入 Prompt → 上下文膨胀 → 更容易超时 → 再次重试更慢 | 发现重试机制的"毒药效应" | 仅调大超时时间——加速膨胀 |
| N3 | 横向对比 | 和 Bug-015 什么关系？同一根因不同表现？ | 同根：KV Cache 不足。Bug-015 是单次请求的 FC 退化，Bug-016 是重试循环的级联恶化 | 确认两个 Bug 必须用同一方案修复 | — |
| N4 | 方案分支 | 三层防御 vs 禁用重试？ | 禁用重试过于激进（正常场景仍需重试），三层防御从根因阻断 | 选三层防御（-sps 0.0 + TokenBudget + Per-Case） | 禁用重试：正常超时场景也失去恢复能力 |
| N5 | 实现决策 | TokenBudgetManager 如何设计？ | 在 LLM 调用前计算预估 token 数，超预算则裁剪 prompt | 预算检查前置，避免进入不可恢复状态 | 事后检测——token 已超限时已晚 |

---

### Bug-014：UTF-8 控制台编码三层修复（锟斤拷乱码）{#bug-014}

| 字段 | 内容 |
|------|------|
| **发现日期** | 2026-06-30 |
| **严重等级** | 🔴 P0 — 全系统日志可读性受损 |
| **发现场景** | CPU 降级模式全量测试 — 所有 `.log` 文件中文输出变为「锟斤拷」乱码 |
| **影响模块** | Agent1/Program.cs, Agent1.Api/Program.cs, Agent1.Tests/ModuleInitializer.cs, 所有 Console/Serilog 输出 |
| **根因** | Windows 中文版默认 `Console.OutputEncoding = GB2312(CP936)`，所有 UTF-8 输出被误解码。三层传播：① Console.WriteLine → ② Serilog Console Sink → ③ 子进程（dotnet test / TRX Logger）继承错误代码页 |
| **修复** | 三层强制 UTF-8：① `Program.cs` 启动时 `Console.OutputEncoding = UTF8`；② `Agent1.Api/Program.cs` 同上；③ `Agent1.Tests/ModuleInitializer.cs` 使用 `[ModuleInitializer]` 钩子在测试程序集加载时设置 |
| **修复提交** | `bff7ee21`, `779a8b05` |
| **关联 Bug** | 无前序 |
| **验证方法** | 检查 `.log` 文件前 3 行不含「锟斤拷」等乱码字符；`grep "锟斤拷" *.log` 返回空 |
| **教训** | UTF-8 编码问题必须从入口层（Program.cs）强制，不能仅靠 Serilog 配置（TextWriter 已经被 Console.Out 的编码器污染） |
| **追问深度** | 5 层（现象 → PowerShell GBK 误解码 → 子进程编码继承 → TRX Logger 污染 → 进程入口控制） |

## 思维链路（对话复盘）

> 记录本次 Bug 分析的关键思考节点，使后续复盘时不依赖记忆即可还原推理过程。

| # | 节点类型 | 触发问题 / AI 追问 | 关键发现 | 决策 | 否决的路径 |
|:---:|------|------|------|------|------|
| N1 | 现象确认 | CPU 降级模式全量测试，所有 .log 文件中文变"锟斤拷" | 测试 PASS/FAIL 不受影响——是显示层问题，不是逻辑问题 | 确认是编码问题而非功能问题 | — |
| N2 | L1→L2 追问 | 为什么只在 PowerShell 管道输出时乱码？不是文件写入时？ | PowerShell 默认 `Console.OutputEncoding = GB2312(CP936)`，中文 Windows 系统默认代码页 | 定位到环境层而非代码层 | 怀疑 Serilog 配置——被"管道才乱码"排除 |
| N3 | 方案分支 | 改 appsettings.json 的 Serilog 编码 vs 改 Program.cs 入口层编码？ | Serilog TextWriter 已被 Console.Out 的编码器污染——配置层改不动 | 选入口层：`Console.OutputEncoding = UTF8` 在 Program.cs 启动时强制 | 配置文件方案：修改无效 |
| N4 | L2→L3 追问 | 为什么 TRX 文件的 `<StdOut>` 也乱码？ | `dotnet test` 子进程继承父进程编码 → TRX Logger 通过 Console.Out 捕获时已被 GBK 污染 | 发现编码污染的三层传播机制 | — |
| N5 | 实现决策 | 测试项目如何修复？ | `[ModuleInitializer]` 在程序集加载时最先执行，比任何测试代码更早设置编码 | 三层统一：Program.cs + Api + ModuleInitializer | 逐文件修改——不可靠且易遗漏 |

---

### Bug-013：Qwen3:8b GB 标准编号幻觉 {#bug-013}

| 字段 | 内容 |
|------|------|
| **发现日期** | 2026-06-30 |
| **严重等级** | 🟡 P1 — 合规结论准确性受损 |
| **发现场景** | T13 合规评测集 — 多个用例 GB 编号错误：硝酸铵→GB 30000.14（应为.15+.2）、丙酮→GB3025（缺零）、甲苯→GB300026（多余零） |
| **影响模块** | ReflectionVerifier.cs, EvalEngine.cs, ChemicalSubstanceDatabase.cs |
| **根因** | Qwen3:8b 从预训练知识生成 GB 编号而非查询数据库，导致：① 格式错误（缺零/多余零）② 子编号映射错误（14→15）③ 完全编造不存在的编号 |
| **修复** | 新增 `ReflectionVerifier.ValidateGbNumberHallucinations()` — 正则提取 GB 30000.XX → 格式纠错(GB3025→GB 30000.25) → 数据库交叉验证 → 集成到自纠错 Prompt 和 EvalEngine 忠实度评分 |
| **修复提交** | `10fb29ae` |
| **关联 Bug** | 无前序，与 [Bug-011/012](#bug-011) 同属 LLM 输出质量问题族 |
| **验证方法** | T13 评测 Citation Accuracy 是否提升（目标 > 80%）；`grep "✗ GB\|→ GB" T13*.log` |
| **教训** | Qwen3:8b 对精确编号（GB 30000 子编号）的幻觉率高，必须代码级后校验而非信任 LLM 输出 |
| **追问深度** | 3 层（现象 → 预训练知识 vs 数据库查询 → 后校验机制设计） |

## 思维链路（对话复盘）

> 记录本次 Bug 分析的关键思考节点，使后续复盘时不依赖记忆即可还原推理过程。

| # | 节点类型 | 触发问题 / AI 追问 | 关键发现 | 决策 | 否决的路径 |
|:---:|------|------|------|------|------|
| N1 | 现象确认 | T13 评测多 case GB 编号错误：硝酸铵→GB 30000.14（应为.15+.2）、丙酮→GB3025（缺零） | 三种错误模式：格式错、子编号错、完全编造——说明不是偶然 | 确认是模型幻觉的系统性表现 | — |
| N2 | L1→L2 追问 | 为什么模型不查询数据库？Qwen3:8b 从哪里生成编号？ | 小模型从预训练知识生成编号，不遵循工具调用指令 | 确认根因在模型行为层 | 加大 Prompt 约束——改不动模型行为 |
| N3 | 方案分支 | 代码级后校验 vs 改 Prompt 强制数据库查询？ | Prompt 修改不可靠，小模型倾向于从预训练记忆回答 | 选代码级后校验：正则提取 → 格式纠错 → 数据库交叉验证 | Prompt 方案：已验证无效 |

---

### Bug-012：T6 RAG 数据校验步骤死循环 {#bug-012}

| 字段 | 内容 |
|------|------|
| **发现日期** | 2026-06-30 |
| **严重等级** | 🔴 P0 — 单次请求耗尽 82.2s + 大量 token |
| **发现场景** | 远程 AutoDL 容器回归测试 — RAG Step 5 校验"是否通过"时，模型反复输出"不通过，回答中引用了知识库中不存在的法规条款..."200+ 次 |
| **影响模块** | LlmService.cs (InvokeStreamAsync), RAG.cs, RAGModule.cs |
| **根因** | `InvokeStreamAsync` 仅检测"是否合规"语义重复（窄检测），未能识别校验语句的结构性重复。Qwen3:8b 在校验失败时陷入「输出否定判断 → 继续重复」循环 |
| **修复** | 替换为 3 维度通用死循环检测：① 连续相同行 >8 次 → 截断；② 字符级联（`)`/`）`/`】`）>12 个 → 截断；③ 总输出 >5000 字符 → 硬截断 |
| **修复提交** | `10fb29ae` |
| **关联 Bug** | [Bug-011](#bug-011) 同一根因（流式输出无通用截断），[Bug-013](#bug-013) 同属 LLM 输出质量族 |
| **验证方法** | T6 日志中无 `⚠️ [截断]` 且无 `⚠️ [慢请求告警]`；耗时 < 60s |
| **教训** | 死循环检测必须通用化（检测输出模式而非语义），不同 Prompt 会触发不同形式的重复 |
| **追问深度** | 3 层（现象 → 窄检测缺陷 → 三维度通用截断设计） |

## 思维链路（对话复盘）

> 记录本次 Bug 分析的关键思考节点，使后续复盘时不依赖记忆即可还原推理过程。

| # | 节点类型 | 触发问题 / AI 追问 | 关键发现 | 决策 | 否决的路径 |
|:---:|------|------|------|------|------|
| N1 | 现象确认 | 远程回归测试 T6 RAG 校验，模型输出"不通过..."200+ 次，耗时 82.2s | 旧版死循环检测未触发——说明检测逻辑有缺陷 | 确认旧检测方案不可靠 | — |
| N2 | L1→L2 追问 | 为什么旧检测没拦住？InvokeStreamAsync 的检测逻辑是什么？ | 窄检测只匹配"是否合规"语义，校验语句"不通过"不在此列 | 发现窄检测的本质缺陷：按语义匹配而非模式匹配 | 只加"不通过"关键词——下次不同 Prompt 还是会漏 |
| N3 | 方案分支 | 语义检测（加关键词）vs 通用模式检测（行重复+字符级联+长度）？ | 语义检测需要为每种 Prompt 定制，不可维护 | 选三维度通用检测：D1 连续相同行 >8 次 + D2 字符级联 >12 + D3 总长度 >5000 | 语义检测：维护成本高、覆盖不完整 |

---

### Bug-011：T5 Reflection 自纠错死循环 {#bug-011}

| 字段 | 内容 |
|------|------|
| **发现日期** | 2026-06-30 |
| **严重等级** | 🔴 P0 — 修正文本重复数百次 + 括号级联 |
| **发现场景** | 远程 AutoDL 容器回归测试 — Reflection 模块"代码级事实核查"阶段，修正内容被反复输出数百次，每行末尾 `)` 从 `))` 增长到 `))))))))......` |
| **影响模块** | LlmService.cs (InvokeStreamAsync), RunReflectionStreamTools.cs, ReflectionModule.cs |
| **根因** | 与 [Bug-012](#bug-012) 相同：`InvokeStreamAsync` 窄检测只匹配"是否合规"，未检测 Reflection 修正文本的重复。Qwen3:8b 在输出修正建议时陷入「输出修正 → 追加右括号 → 重复」的级联循环 |
| **修复** | 同上 3 维度通用死循环检测（D1 重复行 + D2 字符级联 + D3 总长度硬截断） |
| **修复提交** | `10fb29ae` |
| **关联 Bug** | [Bug-012](#bug-012) 同一根因 |
| **验证方法** | T5 日志中无 `⚠️ [截断] 检测到字符重复级联` 信号；Reflection 修正输出行数 < 20 |
| **教训** | 字符级联（`)`/`）`/`】`）是一种独特的死循环模式，与语义重复不同，需要独立检测维度 |
| **追问深度** | 3 层（现象 → 窄检测缺陷 → 字符级联作为独立模式发现） |

## 思维链路（对话复盘）

> 记录本次 Bug 分析的关键思考节点，使后续复盘时不依赖记忆即可还原推理过程。

| # | 节点类型 | 触发问题 / AI 追问 | 关键发现 | 决策 | 否决的路径 |
|:---:|------|------|------|------|------|
| N1 | 现象确认 | 远程回归测试 T5 Reflection 死循环，修正文本重复数百次 + 括号级联 `)` → `))` → `))))))...` | 死循环模式与 T6 不同——不是语义重复，是字符级联 | 确认需要不同的检测维度 | — |
| N2 | 横向对比 | 和 Bug-012 (T6 RAG 死循环) 同一根因？ | 同根：InvokeStreamAsync 窄检测。但表现不同：T6 是语义重复，T5 是字符级联 | 验证同一修复方案（三维度检测）覆盖两种模式 | 分开修——同根应统一修复 |
| N3 | 实现决策 | 字符级联检测的阈值是多少？ | 右括号 `)`/`）`/`】` 连续 >12 个视为级联——正常输出不会出现 | 字符级联独立作为 D2 维度，与 D1 行级检测互补 | 只用 D1 去重——字符级联每行不同，会漏检 |

---

### Bug-010：T13 评分性能+忠实度格式匹配双修复 {#bug-010}

| 字段 | 内容 |
|------|------|
| **发现日期** | 2026-06-29 |
| **严重等级** | 🟡 P1 — 评测分数失真 |
| **发现场景** | T13 合规评测集 — 模型输出格式变化后，`Faithfulness` 评分正则匹配失败，导致分数归零 |
| **影响模块** | EvalEngine.cs, EvalModels.cs |
| **根因** | ① 忠实度评分正则只能匹配旧格式，模型换行/空格变化后匹配失效；② 50 条评测耗时超过固定超时（180s）导致部分用例截断 |
| **修复** | ① 格式匹配增强（兼容新旧格式）；② 新增 T13 心跳检测机制 + 安全上限 3600s |
| **修复提交** | `99bdfa49`, `8de454ec`, `519b3faa` |
| **关联 Bug** | — |
| **验证方法** | T13 Faithfulness 分数合理（非 0 或 1.0 极端值）；50 条全部完成无截断 |
| **教训** | 评测正则必须宽松匹配（兼容模型输出格式漂移），固定超时不适合条数变化的评测集 |
| **追问深度** | 2 层（现象 → 格式漂移根因 + 固定超时设计缺陷） |

## 思维链路（对话复盘）

> 记录本次 Bug 分析的关键思考节点，使后续复盘时不依赖记忆即可还原推理过程。

| # | 节点类型 | 触发问题 / AI 追问 | 关键发现 | 决策 | 否决的路径 |
|:---:|------|------|------|------|------|
| N1 | 现象确认 | T13 Faithfulness 评分归零，50 条评测耗时超过 180s 固定超时 | 两个独立问题：格式匹配失效 + 固定超时不适配 | 拆分为两个子问题分别处理 | — |
| N2 | 方案分支 | 格式问题：宽松匹配 vs 强制 Prompt 锁定输出格式？ | Prompt 无法完全控制模型输出格式，输出会随上下文漂移 | 选宽松匹配：同时兼容新旧两种格式 | Prompt 方案：多次验证不可靠 |
| N3 | 实现决策 | 超时问题：固定值 vs 动态适应？ | 评测集条数可变，固定 180s 不适合 | 新增 T13 心跳检测 + 安全上限 3600s | 单纯放大固定值：下次条数变化仍需调整 |

---

### Bug-009：InsertSubstance 首条记录 tx 参数重复 {#bug-009}

| 字段 | 内容 |
|------|------|
| **发现日期** | 2026-06-29 |
| **严重等级** | 🔴 P0 — 数据库写入失败 |
| **发现场景** | 数据库初始化 — 首条化学品记录插入时 tx 参数被重复传递 |
| **影响模块** | ChemicalDatabaseService.cs |
| **根因** | `InsertSubstance(conn, tx, tx, ...)` tx 参数在调用中重复两次，导致参数绑定错误 |
| **修复** | 修正为 `InsertSubstance(conn, tx, ...)` |
| **修复提交** | `0021ee38` |
| **关联 Bug** | [Bug-008](#bug-008) 同属数据库事务问题族 |
| **验证方法** | 数据库初始化无异常；58 种危化品全部入库 |
| **教训** | 多参数方法调用是手工错误高发区，应考虑使用具名参数或 Builder 模式 |
| **追问深度** | 2 层（现象 → 多参数接口设计缺陷） |

## 思维链路（对话复盘）

> 记录本次 Bug 分析的关键思考节点，使后续复盘时不依赖记忆即可还原推理过程。

| # | 节点类型 | 触发问题 / AI 追问 | 关键发现 | 决策 | 否决的路径 |
|:---:|------|------|------|------|------|
| N1 | 现象确认 | 数据库初始化时首条化学品记录插入失败 | 仅首条失败——后续正常，说明不是系统性故障 | 精确定位到首条的特殊调用路径 | — |
| N2 | L1→L2 追问 | 为什么 tx 参数会被重复传递？ | `InsertSubstance(conn, tx, tx, ...)` 手工复制粘贴错误，多参数方法缺少编译器保护 | 确认是编码规范问题而非逻辑问题 | 只修这一个调用——其他类似位置可能还有 |

---

### Bug-008：数据库事务未正确提交（P0-7）{#bug-008}

| 字段 | 内容 |
|------|------|
| **发现日期** | 2026-06-29 |
| **严重等级** | 🔴 P0 — 数据丢失 |
| **发现场景** | 全项目代码扫描 — 多处 SQLite 事务未正确提交，导致数据写入丢失 |
| **影响模块** | ChemicalDatabaseService.cs, DatabaseService.cs |
| **根因** | `SqliteCommand` 在显式事务中执行时未传入 `transaction` 参数，导致命令在事务外执行 |
| **修复** | 所有显式事务中的 `SqliteCommand` 统一传入 `transaction` 参数 |
| **修复提交** | `1a0cf36f` |
| **关联 Bug** | [Bug-009](#bug-009) 同属数据库事务问题族 |
| **验证方法** | 数据库写入后立即读取验证；事务完整性测试通过 |
| **教训** | SQLite 显式事务中必须显式传入 transaction 参数，否则静默失败（不抛异常但数据丢失） |
| **追问深度** | 4 层（现象 → SQLite 事务隔离机制 → 设计哲学倾向灵活性 → 静默失败场景分析） |

## 思维链路（对话复盘）

> 记录本次 Bug 分析的关键思考节点，使后续复盘时不依赖记忆即可还原推理过程。

| # | 节点类型 | 触发问题 / AI 追问 | 关键发现 | 决策 | 否决的路径 |
|:---:|------|------|------|------|------|
| N1 | 现象确认 | 数据库写入后读取发现数据丢失，无异常日志 | 最危险的模式——静默失败，无法通过异常捕获发现 | 升级为 P0 级严重性 | — |
| N2 | L1→L2 追问 | 为什么不抛异常？SqliteCommand 在事务外执行时行为？ | SQLite 设计哲学：灵活性优先于安全性，允许混合事务和自动提交模式 | 理解了设计决策而非代码 bug | 加 try-catch——SQLite 不会抛异常 |
| N3 | L2→L3 追问 | 为什么 SQLite 选择"灵活性优先"而非"安全优先"？ | 嵌入式数据库定位：零配置、轻量级、兼容性优先 | 确认这是设计约束，修复只能从调用层入手 | — |
| N4 | 方案分支 | 所有 SqliteCommand 统一传 tx vs 改为自动提交模式？ | 事务完整性优先，统一传 tx 确保所有写入在同一事务边界内 | 选显式传 tx：事务完整性不可妥协 | 自动提交：每条 INSERT 独立提交，无法回滚 |

---

### Bug-007：正则表达式 `>=s*` 安全距离提取失效 {#bug-007}

| 字段 | 内容 |
|------|------|
| **发现日期** | 2026-06-16 |
| **严重等级** | 🔴 P0 — 安全距离功能完全失效 |
| **发现场景** | 全项目代码扫描 P0-1 |
| **影响模块** | ChemicalComplianceTools.cs L334 |
| **根因** | 正则表达式漏写反斜杠：`>=s*` 应为 `>=\s*`，导致所有安全距离提取返回空 |
| **修复** | `>=s*` → `>=\s*` |
| **修复提交** | P0 级 Bug 系统性修复批次 |
| **关联 Bug** | — |
| **验证方法** | 安全距离查询返回非空值 |
| **教训** | 正则表达式应使用 `[GeneratedRegex]` 编译时校验 + 单元测试覆盖 |
| **追问深度** | 2 层（现象 → 编译时校验缺失） |

## 思维链路

| # | 节点类型 | 关键发现 | 决策 |
|:---:|------|------|------|
| N1 | 现象确认 | 全项目代码扫描 P0-1：安全距离提取全部返回空 | 确认是单字符遗漏导致功能完全失效 |
| N2 | L1→L2 追问 | `>=s*` vs `>=\\s*`——一个反斜杠的区别，肉眼难以发现 | 正则表达式应使用 `[GeneratedRegex]` 编译时校验 |

---

### Bug-006：Timer(async void) 并发同步冲突 {#bug-006}

| 字段 | 内容 |
|------|------|
| **发现日期** | 2026-06-16 |
| **严重等级** | 🔴 P0 — GPU 向量索引并发崩溃 |
| **发现场景** | 全项目代码扫描 P0-3 |
| **影响模块** | GpuVectorIndexService.cs L242 |
| **根因** | `Timer(async void)` 无法跟踪异步操作完成状态，高并发下多个回调同时修改共享数据结构 |
| **修复** | 替换为 `PeriodicTimer` + `Task.Run`，确保每次只有一个重建任务在执行 |
| **修复提交** | P0 级 Bug 系统性修复批次 |
| **关联 Bug** | — |
| **验证方法** | 高并发向量检索场景无崩溃；GPU 索引重建期间查询不中断 |
| **教训** | 禁止使用 `async void`（除事件处理器外），`Timer` 应替换为 `PeriodicTimer` |
| **追问深度** | 3 层（现象 → async void 异常吞没 → Timer 异步模型缺陷） |

## 思维链路

| # | 节点类型 | 关键发现 | 决策 |
|:---:|------|------|------|
| N1 | 现象确认 | 全项目代码扫描 P0-3：GPU 向量索引并发崩溃 | Timer(async void) 无法跟踪异步完成状态 |
| N2 | 方案分支 | PeriodicTimer + Task.Run vs 加锁？ | 选 PeriodicTimer：从模型层面消除并发冲突，而非靠锁规避 |

---

### Bug-005：BM25 `_avgDocLength` 除零 {#bug-005}

| 字段 | 内容 |
|------|------|
| **发现日期** | 2026-06-16 |
| **严重等级** | 🟡 P1 — 空知识库时 BM25 崩溃 |
| **发现场景** | 全项目代码扫描 P1-5 |
| **影响模块** | KnowledgeBaseService.cs L190 |
| **根因** | 知识库为空时 `_avgDocLength = 0`，BM25 公式中除零导致 `NaN` 得分 |
| **修复** | `Math.Max(1.0, _avgDocLength)` 除零保护 |
| **修复提交** | P0 级 Bug 系统性修复批次 |
| **关联 Bug** | — |
| **验证方法** | 空知识库环境（首次安装）不崩溃 |
| **教训** | 所有除法运算必须有除零保护，尤其是动态计算的平均值变量 |
| **追问深度** | 2 层（现象 → 动态平均值边界条件保护缺失） |

## 思维链路

| # | 节点类型 | 关键发现 | 决策 |
|:---:|------|------|------|
| N1 | 现象确认 | 全项目代码扫描 P1-5：空知识库时 BM25 崩溃 | `_avgDocLength = 0`，BM25 公式分母为零 → NaN |
| N2 | 实现决策 | `Math.Max(1.0, _avgDocLength)` 单行修复 | 所有除法运算必须有除零保护——教训通用化 |

---

### Bug-004：ReflectionVerifier 检索异常错误标记 {#bug-004}

| 字段 | 内容 |
|------|------|
| **发现日期** | 2026-06-16 |
| **严重等级** | 🟡 P1 — Reflection 误判幻觉 |
| **发现场景** | 全项目代码扫描 P1-9 |
| **影响模块** | ReflectionVerifier.cs L165 |
| **根因** | 知识库检索异常时（如网络超时），代码错误地设置 `FoundInSource=true`，导致"幻觉"内容被误标为"已核实" |
| **修复** | 检索异常时标记 `[KB检索异常]` 而非 `FoundInSource=true` |
| **修复提交** | P0 级 Bug 系统性修复批次 |
| **关联 Bug** | — |
| **验证方法** | 模拟 KB 服务不可用时，Reflection 输出含 `[KB检索异常]` 标记 |
| **教训** | 异常路径的默认值必须安全（fail-safe），不能默认"成功" |
| **追问深度** | 2 层（现象 → fail-safe 设计原则） |

## 思维链路

| # | 节点类型 | 关键发现 | 决策 |
|:---:|------|------|------|
| N1 | 现象确认 | 全项目代码扫描 P1-9：KB 检索异常时错误设置 FoundInSource=true | 异常路径默认值不安全——failure 被当作 success |
| N2 | 实现决策 | 改为 `[KB检索异常]` 标记，遵循 fail-safe 原则 | 异常路径默认值必须是保守的 |

---

### Bug-003：ConclusionVerifier 双格式匹配缺失 {#bug-003}

| 字段 | 内容 |
|------|------|
| **发现日期** | 2026-06-16 |
| **严重等级** | 🔵 P2 — 评测容错性不足 |
| **发现场景** | 全项目代码扫描 P2-12 |
| **影响模块** | ConclusionVerifier.cs L79 |
| **根因** | 仅匹配 `【合规判断】` 格式，不匹配模型输出的替代格式 `[判定:is_compliant=...]` |
| **修复** | 同时匹配 `【合规判断】` 和 `[判定:is_compliant=...]` 两种输出格式 |
| **修复提交** | P0 级 Bug 系统性修复批次 |
| **关联 Bug** | [Bug-010](#bug-010) 同属评测格式匹配问题族 |
| **验证方法** | 两种格式的模型输出均能正确解析 |
| **教训** | 评测正则必须防御性设计，模型输出格式会随着 Prompt 微调而漂移 |
| **追问深度** | 2 层（现象 → 防御性正则设计原则） |

## 思维链路

| # | 节点类型 | 关键发现 | 决策 |
|:---:|------|------|------|
| N1 | 现象确认 | 全项目代码扫描 P2-12：模型新输出格式 `[判定:is_compliant=...]` 无法匹配 | 单一格式假设是脆弱的——模型输出会随上下文变化 |
| N2 | 方案分支 | 同时匹配新旧格式 vs 强制 Prompt 锁定格式？ | 选同时匹配：防御性设计，不依赖 Prompt 能锁住格式 |

---

### Bug-002：RagCache 并发安全问题 {#bug-002}

| 字段 | 内容 |
|------|------|
| **发现日期** | 2026-06-16 |
| **严重等级** | 🔴 P0 — 高并发下数据损坏 |
| **发现场景** | 全项目代码扫描 P0-2 |
| **影响模块** | ChemicalComplianceTools.cs L49 |
| **根因** | `RagCache` 使用普通 `Dictionary`，多线程并发读写导致数据损坏 |
| **修复** | 升级为 `ConcurrentDictionary` + TTL + LRU 淘汰 |
| **修复提交** | P0 级 Bug 系统性修复批次 |
| **关联 Bug** | — |
| **验证方法** | 压力测试下缓存命中率正常，无异常 |
| **教训** | 任何可能被多线程访问的共享数据结构必须使用并发安全类型 |
| **追问深度** | 2 层（现象 → 并发安全类型选择原则） |

## 思维链路

| # | 节点类型 | 关键发现 | 决策 |
|:---:|------|------|------|
| N1 | 现象确认 | 全项目代码扫描 P0-2：多线程并发读写 Dictionary 致数据损坏 | 普通 Dictionary 不保证线程安全 |
| N2 | 方案分支 | ConcurrentDictionary + TTL + LRU vs 全局加锁？ | 选 ConcurrentDictionary：专为并发设计，性能优于全局锁 |

---

### Bug-001：RRF 去重键非确定性 {#bug-001}

| 字段 | 内容 |
|------|------|
| **发现日期** | 2026-06-16 |
| **严重等级** | 🔴 P0 — 检索结果不稳定 |
| **发现场景** | 全项目代码扫描 P0-4 |
| **影响模块** | HybridKnowledgeBaseService.cs L527 |
| **根因** | RRF 融合去重键使用 `Guid.NewGuid()`（每次调用生成不同值），导致同一文档不能被正确去重，检索结果不稳定 |
| **修复** | 改为确定性去重键 `GetDedupKey()`（基于文档路径+chunk索引） |
| **修复提交** | P0 级 Bug 系统性修复批次 |
| **关联 Bug** | — |
| **验证方法** | 同一查询多次执行返回相同结果集 |
| **教训** | 去重/幂等操作中的标识符必须是确定性的（基于内容语义），不能用随机值 |
| **追问深度** | 3 层（现象 → Guid 随机性 vs 确定性标识符 → 幂等性设计原则） |

## 思维链路

| # | 节点类型 | 关键发现 | 决策 |
|:---:|------|------|------|
| N1 | 现象确认 | 全项目代码扫描 P0-4：同一查询多次执行返回不同结果 | Guid.NewGuid() 每次生成不同值——去重键不具幂等性 |
| N2 | L1→L2 追问 | 为什么去重需要确定性标识符？ | 幂等操作要求：相同输入 → 相同输出。随机值破坏这一契约 |
| N3 | 实现决策 | 改为 `GetDedupKey()` 基于文档路径+chunk索引 | 标识符必须从内容语义推导，不能用随机值 |

---

### Bug-018：FunctionChoiceBehavior.Required() 导致评测死循环 {#bug-018}

| 字段 | 内容 |
|------|------|
| **发现日期** | 2026-07-02 |
| **严重等级** | 🔴 P0 — 评测卡死，单条用例永不完结 |
| **发现场景** | T13 评测集 Bug-017 修复后重跑 — F005（乙醇+硝酸综合审核）时 LLM 持续调用 CheckStorageCompatibility 40+ 次 |
| **影响模块** | `Agent1/Services/AI/LlmService.cs` L615 |
| **根因** | `FunctionChoiceBehavior.Required()` 每轮 HTTP 请求都强制 LLM 调用工具。SK 内部 FC 循环：工具返回结果后自动发起下一轮 HTTP 请求 → Required() 再次生效 → LLM 被迫再次调用工具 → 无限循环。LLM 已生成正确答案但被 Required() 剥夺了"自主终止权"。三层死循环检测（D1重复行/D2字符级联/D3总字符截断）均未拦住，因为每轮文本不同、中文无括号级联、截断触发太晚。 |
| **修复** | `FunctionChoiceBehavior.Required()` → `FunctionChoiceBehavior.Auto()`，业务评测由 LLM 自主决定是否调用工具。Required() 仅保留在 FC 就绪性检查（`InvokeNonStreamingWithRetryAsync` L467）。 |
| **修复提交** | 待提交 |
| **关联 Bug** | [Bug-017](#bug-017)（Bug-017 隐藏了 Bug-018，修完前者才暴露后者） |
| **验证方法** | T13 全量评测跑通，无单一工具连续调用 >5 次 |
| **教训** | 评测有两种模式需要分离：FC 就绪检查（需要 Required() 强制验证管线）vs 业务评测（需要 Auto() 让 LLM 自主决策）。同一个策略不能同时用于两个目的不同的场景。 |
| **追问深度** | 3 层（代码配置 → SK 内部 FC 循环 → 设计冲突） |

## 思维链路（对话复盘）

> 记录本次 Bug 分析的关键思考节点，使后续复盘时不依赖记忆即可还原推理过程。

| # | 节点类型 | 触发问题 / AI 追问 | 关键发现 | 决策 | 否决的路径 |
|:---:|------|------|------|------|------|
| N1 | 现象确认 | Bug-017 修复后重跑 T13，F005 卡死(40+ 次 CheckStorageCompatibility) | Bug-017 的崩溃掩盖了 Bug-018——修完前者才暴露后者 | 确认新 Bug，启动独立分析 | — |
| N2 | L1→L2 追问 | 为什么同一工具被反复调用？Required() 的行为机制是什么？ | SK 内部 FC 循环：工具返回后自动发起下一轮 HTTP 请求 → Required() 再次生效 → LLM 被迫再次调用工具 | 深入 SK 框架 FC 循环机制，而非仅在代码层看 | 停在"加超时"或"限制调用次数"的治标方案 |
| N3 | 方案分支 | 方案 A：业务评测改为 Auto() vs 方案 B：缩短超时 + 限制调用次数 | A 从机制层解决（LLM 自主终止权），B 仅延缓症状 | 选 A：Required() → Auto()，Required() 仅保留在 FC 就绪检查 | B：治标不治本，不同 case 需要不同次数 |
| N4 | 实现决策 | Required() 保留在哪里？ | FC 就绪性检查（InvokeNonStreamingWithRetryAsync L467）需强制验证管线，业务评测需 LLM 自主决策 | 两种场景分离：检查用 Required，评测用 Auto | 全改 Auto：FC 就绪性检查失去强制验证能力 |

---

### Bug-019：HyDE + FunctionChoiceBehavior.Required() 导致 FC 递归死循环（F005 静默卡死）{#bug-019}

| 字段 | 内容 |
|------|------|
| **发现日期** | 2026-07-03 |
| **严重等级** | 🔴 P0 — 静默卡死，进程存活但无进度，比 Bug-018 更隐蔽 |
| **发现场景** | T13 心跳监控日志（t13_heartbeat.log）— F005（乙醇+硝酸综合审核）卡死在 `CheckStorageCompatibility → HyDE文档检索 → CheckStorageCompatibility` 无限递归，单 case 超 1800+ 行日志无任何结论输出 |
| **影响模块** | `Agent1/Services/AI/LlmService.cs` L124（FC 硬编码）、`Agent1/Services/AI/ILlmService.cs`（接口缺 FC 参数）、`Agent1/Services/Knowledge/HybridKnowledgeBaseService.cs`（HyDE 调用方）、`Agent1/Services/Dialog/RunReflectionStreamTools.cs`（Reflection 调用方） |
| **根因** | **L1 现象**：进程存活(PID 2929)但不产出结论，同一 case 中 CheckStorageCompatibility 被反复调用；**L2 调用链**：主管线 FC=Required → CheckStorageCompatibility DB 未命中 → HyDeRetrieveIfAvailableAsync → HydeRetrieveAsync → InvokeStreamWithRetryAsync(hydePrompt) → InvokeStreamAsync 硬编码 `FunctionChoiceBehavior.Required()` → HyDE 提示词「生成法规摘要…乙醇硝酸同库储存」被强制调用工具 → 再次触发 CheckStorageCompatibility → 无限递归。Bug-018 是显性死循环（同一工具 40+ 次、会超时），Bug C 是**静默卡死**（进程永远不死、监控告警不触发、每轮工具名交替变化）；**L3 设计冲突**：Git 历史 commit `82dae1dd` FC 统一化时主动移除了 FC 灵活性，HyDE 被引入时 (`40bcb0fa`) 被动继承了 Required()，形成「FC 统一 + HyDE 特殊需求」的设计冲突。 |
| **修复** | **FC 策略参数化架构重构**（4 文件修改）：① `ILlmService.cs` 新增 3 个带 `FunctionChoiceBehavior? fcBehavior = null` 参数的重载，默认 null → Required() 向后兼容；② `LlmService.cs` 核心实现：`InvokeStreamAsync` / `InvokeStreamWithRetryAsync` / `InvokeNonStreamingWithRetryAsync` 全部 FC 参数化，新增 effectiveFc = fcBehavior ?? Required()；③ `HybridKnowledgeBaseService.cs` HyDE 两条路径（流式+降级）改为 `FunctionChoiceBehavior.None()` 从根源阻断递归；④ `RunReflectionStreamTools.cs` 分层策略：Step1→Auto()（允许但不强制）、Step4/5/降级→None()（手动 ParseToolCalls 模式不依赖 SK FC） |
| **修复提交** | 待提交 |
| **关联 Bug** | [Bug-018](#bug-018)（同一 FC 配置族的显性表现）、[Bug-016](#bug-016)（同属静默卡死但根因是 KV Cache） |
| **验证方法** | T13 心跳监控中 F005 无 1800+ 行重试日志；`grep "Required\|None" t13*.log` FC 策略分布符合预期（主路径 Required、HyDE 路径 None、Reflection 路径 Auto/None） |
| **教训** | ① 静默卡死比显性死循环更危险：进程存活、监控不告警、难以发现；② FC 策略是**语义级参数**，不同调用目的（工具辅助检索 vs 管线强制调用）对 FC 有不同需求，API 设计时必须参数化不能硬编码；③ 统一化重构时需检查所有 call site 是否有差异化需求，统一 ≠ 同质。 |
| **追问深度** | 3 层（L1 现象 → L2 调用链递归机制 → L3 设计冲突与 Git 历史演进） |

## 思维链路（对话复盘）

> 记录本次 Bug 分析的关键思考节点，使后续复盘时不依赖记忆即可还原推理过程。

| # | 节点类型 | 触发问题 / AI 追问 | 关键发现 | 决策 | 否决的路径 |
|:---:|------|------|------|------|------|
| N1 | 现象确认 | t13_heartbeat.log 中 F005 卡死在 1800+ 行循环 | 进程存活(PID 2929)但不产出结论，CheckStorageCompatibility 被反复调用 | 确认 Bug C 真实存在，进入 L1 分析 | — |
| N2 | L1→L2 追问 | 为什么 HyDE 提示词会触发 CheckStorageCompatibility？ | 调用链：主管线 FC=Required → CheckStorageCompatibility → HyDE → InvokeStreamAsync 硬编码 Required() → 递归 | 深入原理层追踪完整调用链而非修症状 | 停在 L1 修症状（加超时/截断）无法根治递归根因 |
| N3 | 横向对比 | Bug-016 / Bug-018 和 Bug C 有什么异同？ | 同属 FC 配置族：Bug-018 是显性死循环（同一工具 40+ 次、会超时），Bug C 是静默卡死（进程永远不死、工具名交替变化） | 确认 Bug C 比 Bug-018 更隐蔽更危险 | — |
| N4 | L2→L3 追问 | 为什么 InvokeStreamAsync 硬编码 Required()？Git 历史上谁改的？ | commit `82dae1dd` FC 统一化主动移除灵活性，HyDE (`40bcb0fa`) 被动继承 Required()，Bug B 修复 (`0a246d3d`) 只改了评测路径 | 暴露设计冲突：FC 统一 ≠ 所有场景需求相同 | 不追溯 Git 历史就无法理解"为什么当时没发现这个递归问题" |
| N5 | 方案分支 | 方案 A（FC 策略参数化）还是方案 B（HyDE 路径硬编码改 None）？ | A 向后兼容（默认 Required）、所有调用方可控；B 简单但有回归风险（Reflection 路径也被影响） | 选 A：FC 策略参数化架构重构（4 文件修改） | B：硬编码改 None，无法精细控制 Reflection 各步骤的 FC 策略 |
| N6 | 实现决策 | Reflection Step1 用 Auto() 还是 None()？ | Reflection 使用手动 ParseToolCalls 模式，不依赖 SK Auto FC；Step1 需要工具辅助推理 | Step1→Auto（允许但不强制），Step4/5/降级→None（纯审查/修正任务） | 全部 None：Step1 可能跳过工具调用失去推理依据；全部 Auto：与手动 ParseToolCalls 可能冲突 |

---

### Bug-017：字符串插值 Guid 格式说明符歧义导致全量 FormatException {#bug-017}

| 字段 | 内容 |
|------|------|
| **发现日期** | 2026-07-01 |
| **严重等级** | 🔴 P0 — 63 条评测全部崩溃，tool_call_rate=0 |
| **发现场景** | T13 合规评测集执行 — 每条用例第一行即抛出 FormatException，FC 就绪性检查却通过（因为走的是不同代码路径） |
| **影响模块** | `Agent1/Services/Dialog/AgentDialog.cs` L519 |
| **根因** | C# 字符串插值中 `{Guid.NewGuid():N[..6]}` 的 `:` 是格式分隔符，冒号后 `N[..6]` 被编译器整体当作 `Guid.ToString("N[..6]")` 的格式参数。`"N[..6]"` 不是合法格式字符串 → FormatException。正确意图是 `.ToString("N")` 去横杠 + `[..6]` 取前 6 位，但两个操作不能挤在 `:` 后面。 |
| **修复** | 将 `.ToString("N")` 和 `[..6]` 移到插值表达式 `{}` 外部：`Guid.NewGuid().ToString("N")[..6]` |
| **修复提交** | 待提交 |
| **关联 Bug** | [Bug-018](#bug-018)（修复暴露出后续的死循环问题） |
| **验证方法** | 编译通过，63 条评测不再抛 FormatException |
| **教训** | C# `$` 插值中 `:` 之后进入"格式说明符模式"，不再是 C# 表达式上下文。范围运算符 `[..]` 在 `:` 之后不被识别。需要范围切片时，表达式必须在 `{}` 外部完成。 |
| **追问深度** | 4 层（现象 → C# 插值解析规则 → Roslyn 编译器中表示 → C# 语言设计决策） |

## 思维链路（对话复盘）

> 记录本次 Bug 分析的关键思考节点，使后续复盘时不依赖记忆即可还原推理过程。

| # | 节点类型 | 触发问题 / AI 追问 | 关键发现 | 决策 | 否决的路径 |
|:---:|------|------|------|------|------|
| N1 | 现象确认 | T13 63/63 全部 FormatException，tool_call_rate=0 | 全量崩溃但 FC 就绪性检查通过——两个路径走不同代码 | 确认是语法错误而非逻辑错误 | 怀疑是模型或网络问题——被 FC 检查通过排除 |
| N2 | L1→L2 追问 | 为什么 FC 检查通过但业务评测崩溃？代码路径差异在哪？ | FC 检查路径无 session ID 生成逻辑，业务路径 AgentDialog.cs L519 有 `Guid.NewGuid():N[..6]` | 精确定位到插值表达式中的 `:` 语义陷阱 | 在更广范围搜索——已缩小到唯一可疑点 |
| N3 | 横向对比 | C# `:` vs Python f-string `:` 的区别？为什么直觉上"看起来没问题"？ | C# `:` 后进入"格式说明符模式"，`N[..6]` 被整体当作格式参数，而不是 `.ToString("N")` + `[..6]` 两步操作 | 确认是 C# 语言设计层面的知识盲区 | — |
| N4 | 实现决策 | 立即修复 vs 同时抽取公共方法？ | P0 优先止血：`Guid.NewGuid().ToString("N")[..6]` 移到 `{}` 外部 | 暂不重构——Bug-018 还在后面等着被暴露 | 大范围重构风险高、耗时长，P0 不适用 |

---

### Bug-020：info_query 工具白名单不足 — G/H/I 三类 13 条全挂 {#bug-020}

| 字段 | 内容 |
|------|------|
| **发现日期** | 2026-07-03 |
| **严重等级** | 🔴 P0 — 13 条用例工具触发率为 0，全量假阴性 |
| **发现场景** | T13 评测集扩展 G（化学品属性 5 条）、H（重大危险源 5 条）、I（法规版本 3 条）三类后重跑 — 全部 tool_call_rate=0，LLM 绕过工具直接编造答案 |
| **影响模块** | `Agent1/Services/Eval/EvalEngine.cs` L170-175 |
| **根因** | **L1 现象**：G/H/I 三类 13 条用例工具触发全零；**L2 调用链**：EvalEngine L164 `isInfoQuery=true` 判定后，L170-173 白名单仅含 3 个工具（CheckHazardCategory、GetSafetyDistance、GetCurrentTime），G 类需要的 `LookupChemicalProperties`、H 类需要的 `GetMajorHazardThreshold`、I 类需要的 `CheckRegulationVersion` 均不在列表中；**L3 设计冲突**：Token 预算控制（尽量少传工具定义以节省 token）与功能完整性（覆盖全部用例类别）之间存在未解决的张力——白名单扩展与评测集扩展是两个独立变更点，缺少编译时/运行时一致性校验。 |
| **修复** | 白名单从 3 个扩展到 6 个，新增 `LookupChemicalProperties`、`GetMajorHazardThreshold`、`CheckRegulationVersion`。Token 预算从 ~500 增至 ~800（+60%），仍在安全范围。 |
| **修复提交** | 本次 |
| **关联 Bug** | [Bug-010](#bug-010)（同属评测框架边界条件处理不足族）、[Bug-003](#bug-003)（同属评测格式/配置漂移族） |
| **验证方法** | T13 重跑 G/H/I 三类 tool_call_rate > 0；`grep "本轮共调用 0 个工具" T13*.log` 无 G/H/I 类匹配 |
| **教训** | 按意图差异化行为（info_query vs compliance_judgment 工具裁剪）需与用例类别变更联动，当前无自动化一致性校验——未来新增工具类时仍依赖人工同步。 |
| **追问深度** | 3 层（L1 所有 G/H/I 工具触发 0 → L2 白名单仅 3 个工具 → L3 Token 预算 vs 功能完整性的设计冲突 + 编译时一致性校验缺失） |

## 思维链路（对话复盘）

> 记录本次 Bug 分析的关键思考节点，使后续复盘时不依赖记忆即可还原推理过程。

| # | 节点类型 | 触发问题 / AI 追问 | 关键发现 | 决策 | 否决的路径 |
|:---:|------|------|------|------|------|
| N1 | 现象确认 | T13 G/H/I 三类 13/13 tool_call_rate=0 | 三类全挂——不是偶发，是系统性缺失 | 进入根因分析 | — |
| N2 | L1→L2 追问 | 为什么 info_query 不给这三类工具？ | 白名单硬编码 3 个工具，G/H/I 需要的 3 个工具全不在列 | 白名单 3→6 扩展 | — |
| N3 | L2→L3 追问 | 为什么评测集扩展时没同步更新白名单？ | 用例扩展（ComplianceEvalSet.json）与引擎工具裁剪（EvalEngine.cs）是独立变更点，无自动化一致性校验 | 确认当前设计缺少编译时校验——登记为技术债 | 停下修复后不追究——L3 追问暴露了更深层的架构问题 |

---

### Bug-021：C 类结论判定过松 — 缺少 Level 4 反向幻觉扣除 {#bug-021}

| 字段 | 内容 |
|------|------|
| **发现日期** | 2026-07-03 |
| **严重等级** | 🟡 P1 — 结论准确率仅 33%（15 条中 10 条误判通过） |
| **发现场景** | T13 评测集中 C 类（危险类别查询）15 条用例结论准确率仅 33%——日志显示大量用例被标记为"✅ 结论通过"，但实际 LLM 输出了错误的 GB 编号 |
| **影响模块** | `Agent1/Services/Eval/EvalEngine.cs` L1093-1118（CheckConclusion）、L237-269（主循环 KB 验证） |
| **根因** | **L1 现象**：C 类结论准确率 33%，10/15 失败；**L2 调用链**：`CheckRegulationMatch` 在 L1093-1098 仅做子串 `Contains` 检查——去除 "GB " 前缀后判断预期编号是否在 response 中任意位置出现。不验证该编号是否是真正的 GB 标准编号、也不管回答中是否同时编造了其他不存在的编号。Qwen3:8b 在信息不足时倾向"猜测"GB 编号而非诚实说不知道，宽松判定进一步鼓励了这种行为；**L3 设计冲突**：评测框架的"宽容性"（允许部分匹配以应对模型输出格式漂移）vs 安全关键系统的"零幻觉"要求——化工合规场景下，一个错误的 GB 编号可能导致错误的合规结论→错误的安全决策。 |
| **修复** | 两层防御：① `CheckConclusion` 中新增 **Level 4 反向幻觉扣除**：提取 LLM 回答中所有 GB 编号→检查预期编号是否在其中→不在则 fail；② 主循环中对 LLM 回答中额外出现的 GB 编号逐条 KB 验证（`RetrieveChemicalRegulationAsync`）→不在 KB 中的计入 `HallucinatedClaims`。 |
| **修复提交** | 本次 |
| **关联 Bug** | [Bug-013](#bug-013)（同属 LLM GB 编号幻觉族——Bug-013 是模型行为层幻觉，Bug-021 是评测判定层纵容幻觉） |
| **验证方法** | C 类结论准确率从 33% 提升至 85%+；`eval_report.json` 中 `hallucinated_claims` 字段正确记录了幻觉声明 |
| **教训** | 安全关键系统的判定逻辑不能依赖子串匹配——"回答里出现了预期编号" ≠ "回答是正确的"。Level 4 反向扣除是从"被动接受"到"主动验证"的范式转换。 |
| **追问深度** | 3 层（L1 C 类准确率 33%→L2 CheckRegulationMatch 子串匹配过于宽松→L3 评测宽容性 vs 安全零幻觉的冲突） |

## 思维链路（对话复盘）

> 记录本次 Bug 分析的关键思考节点，使后续复盘时不依赖记忆即可还原推理过程。

| # | 节点类型 | 触发问题 / AI 追问 | 关键发现 | 决策 | 否决的路径 |
|:---:|------|------|------|------|------|
| N1 | 现象确认 | C 类 15 条 10 条误判通过——"✅ 结论通过"但 LLM 输出错误 GB 编号 | 33% 准确率与 100% 结论通过率的矛盾——说明判定逻辑有系统性缺陷 | 确认判定逻辑需重构 | — |
| N2 | L1→L2 追问 | 为什么错误编号判定通过？CheckRegulationMatch 做了什么？ | 实现是子串 `Contains`——回答中任意位置出现预期编号子串就通过，不验证编号真实性 | 需要在结论判定后增加反向验证层 | — |
| N3 | 方案分支 | 反向验证放在 static CheckConclusion 还是调用方？ | CheckConclusion 是 static 方法，无法直接访问 KB（需 async）。Level 4 提取+预期匹配放 static 层；KB 逐条验证放调用方 async 路径 | 分两层实现：static 做轻量校验，调用方做重量级 KB 验证 | 全放 static 需要改方法签名为 async——影响面大 |
| N4 | 实现决策 | Level 4 提取所有 GB 编号的粒度？ | 每发现一个不在 KB 中的额外编号 → `HallucinatedClaims++`，精确到每条声明 | 细粒度计入审计数据（ClaimDetails/ConclusionReasons） | 只设粗粒度计数器——丢失逐条审计信息 |

---

### Bug-022：忠实度 + Citation Accuracy 无审计追踪 {#bug-022}

| 字段 | 内容 |
|------|------|
| **发现日期** | 2026-07-03 |
| **严重等级** | 🔵 P2 — 评分不可追溯，无法定位低分原因 |
| **发现场景** | T13 评测集日志审查 — 忠实度 56.6%、Citation Accuracy 45.8%，日志只输出裸数字，无法知道哪条声明被判定为幻觉、哪条引用未在检索源中找到 |
| **影响模块** | `Agent1/Models/EvalModels.cs` L111-166（新增 3 个模型类）+ L274-281（EvalResult 新增字段）、`Agent1/Services/Eval/EvalEngine.cs` L705-708 + L772-797（忠实度遍历）+ L913-920 + L972-1018（Citation 遍历）、`Agent1/Services/AI/ReflectionVerifier.cs` L17-20 + L159-174（ClaimVerification 扩展） |
| **根因** | **L1 现象**：忠实度 56.6%、Citation 45.8%，仅输出汇总数字；**L2 调用链**：`EvaluateFaithfulnessAsync` 只取 `TotalClaims`/`VerifiedClaims`/`HallucinatedClaims` 三个聚合数字——`ReflectionVerifier.VerifyBusinessFactsAsync` 返回的每条声明的检索查询、返回 chunk 数、匹配结果全部丢弃。同理，`EvaluateCitationAccuracy` 只统计 `foundCount`，每条引用的匹配 chunk 信息不保留。`ClaimVerification` 结构缺少 `SearchQuery`/`ChunksReturned` 字段；**L3 设计冲突**：评测框架的"评分导向"（快速出分数）vs 诊断场景的"审计导向"（逐条验证、可复现）。两类需求的数据结构不同——前者需要聚合数字，后者需要保留中间结果的完整 DAG。 |
| **修复** | 三层可追溯化：① **数据模型**：新增 `ClaimDetail`（声明→query→chunks→match 全链路）、`CitationTrace`（引用→chunk_id→snippet 匹配链路）、`ConclusionReason`（判定→Level→规则→结果）三个类，挂载到 `EvalResult`；② **评测方法**：忠实度评估输出每条声明的 `✓/✗` + query + chunks 日志，Citation 评估输出每条引用的匹配状态 + chunk_id 日志；③ **ReflectionVerifier**：`ClaimVerification` 扩展 `SearchQuery`/`ChunksReturned` 字段并在 VerifyBusinessFactsAsync 中填充。 |
| **修复提交** | 本次 |
| **关联 Bug** | [Bug-004](#bug-004)（同属中间信息丢失族——反射验证器异常路径默认"成功"导致幻觉误判）、[Bug-003](#bug-003)（同属评测框架防御性设计不足族） |
| **验证方法** | `eval_report.json` 中 `claim_details`/`citation_traces`/`conclusion_reasons` 字段非空且结构完整；日志每行含逐条声明的 `✓/✗` 标记 |
| **教训** | 评测框架设计需区分"评分模式"（输出分数）和"诊断模式"（输出审计轨迹）——两类场景的数据结构完全不同，必须在设计阶段就考虑审计需求，不能事后补。 |
| **追问深度** | 4 层（L1 裸数字 → L2 中间结果丢弃 → L3 ClaimVerification 结构缺失 → L4 评分导向 vs 审计导向的设计冲突） |

---

### Bug-023：评测路径双通道不完整 — 缺 FactAssembler+兜底导致忠实度未升反降 {#bug-023}

| 字段 | 内容 |
|------|------|
| **发现日期** | 2026-07-03 |
| **严重等级** | 🟡 P1 — 双通道架构核心价值（零幻觉法规引用）在评测路径未生效，忠实度/Citation 未达目标但系统仍可用 |
| **发现场景** | 全量回归测试后用户质疑「为什么双通道架构上线后 T13 评测结果反而下降了？」——忠实度 56.0%（目标 100%）、Citation Accuracy 40.7%（目标 100%），日志中 `DecoupledPipeline` 出现 0 次 |
| **影响模块** | `Agent1/Services/Dialog/AgentDialog.cs` L624-645（评测路径双通道块）+ L392-406（对话路径对照）+ `Agent1/Services/Eval/EvalEngine.cs` L164-175（工具白名单，已单独修复为 Bug-020）+ `Agent1/Services/Compliance/FactAssembler.cs`（依赖但未调用）+ `Agent1/Services/Compliance/ResponseMerger.cs`（依赖但未调用） |
| **根因** | **L1 现象**：忠实度/Citation 显著低于对话路径基线，T13 日志 0 次 DecoupledPipeline；**L2 调用链**：评测路径 L624-645 仅调用 `OutputSanitizer.Sanitize`（消毒 LLM 幻觉引用），缺少 `FactAssembler.Build`（确定性渲染）和 `ResponseMerger.Merge`（双通道合并）——对话路径 L392-406 有完整三件套，评测路径只有一件；**L3 设计冲突**：评测路径作为后加的执行分支，开发时按「先用消毒验证效果」最小策略实现，对话路径的完整双通道是后续迭代上去的——两条路径的代码从未被并排对照检查过对称性；**L4 防御缺失**：`evalToolCalls.Count == 0` 时整个双通道块被跳过（else 分支不存在），LLM 自由输出零保护——而 info_query 白名单缺陷（Bug-020）恰好使 13 条用例命中此空洞，两个 Bug 叠加放大损失 |
| **修复** | 三处改动（[bd4760d3](https://gitee.com/liuchao_yue/agent-system/commit/bd4760d3)）：① 评测路径补全 `FactAssembler.Build(evalFacts)` + `ResponseMerger.Merge(factOutput, sanitizedExplanation)`，与对话路径保持完全一致的三件套；② 新增 else 兜底：`evalFacts.HasAnyToolResult == false` 时先 `OutputSanitizer.Sanitize` 消毒 LLM 输出剔除幻觉引用，再 `FactAssembler.Build` 渲染 BuildNoResult 声明，最后 `ResponseMerger.Merge` 合并；③ 配合 Bug-020 的白名单修复（6个工具）确保 info_query 13 条尽可能触发工具 |
| **修复提交** | `bd4760d3` — fix: 评测路径双通道补全 FactAssembler+兜底，修复忠实度下降 |
| **关联 Bug** | [Bug-020](#bug-020)（白名单缺陷 + 本 Bug 的 else 兜底 = 双重保护：白名单让工具尽可能触发，兜底让即使未触发也有保护）；[Bug-022](#bug-022)（审计追踪使本 Bug 的发现成为可能——无审计则无法定位 DecoupledPipeline=0）；[Bug-021](#bug-021)（判定过松 + 本 Bug 的消毒缺失 = 同一 LLM 输出中既有漏判又有误删） |
| **验证方法** | 远程 T13 重跑（[t13_20260703_161713](file:///d:/桌面/agent/项目/Agent1/logs/linux/自动化脚本测试输出/root/autodl-tmp/test-results/t13_20260703_161713/T13_heartbeat.log)）：工具触发率 74.6%→93.7%（+19.1%）、忠实度 56.0%→74.3%（+18.3%）、Citation 40.7%→44.2%（+3.5%）、综合评级 ★☆☆→★★★、综合分 69.8%→85.7% |
| **教训** | ① **执行路径对称性检查**：任何新增执行路径（对话/评测/API）必须与已有路径做并排对照——同名保护机制在多路径上的差异要么消除、要么在代码注释中明确记录差异原因和承担的风险；② **兜底是架构要求而非锦上添花**：防护机制的条件分支必须有 else——"工具未触发时跳过保护"等于在安全门上留了后门；③ **远程部署与本地代码的版本一致性**：本次 T13 重跑发现远程运行的是旧白名单代码（3 个工具），本地已有 6 个工具——部署流程必须强制验证 git commit hash |
| **追问深度** | 4 层（L1 指标下降 → L2 评测路径缺 FactAssembler+ResponseMerger → L3 对话vs评测路径对称性缺失 → L4 else 分支缺失 + Bug-020 叠加放大） |

## 思维链路（对话复盘）

> 记录本次 Bug 分析的关键思考节点，使后续复盘时不依赖记忆即可还原推理过程。

| # | 节点类型 | 触发问题 / AI 追问 | 关键发现 | 决策 | 否决的路径 |
|:---:|------|------|------|------|------|
| N1 | 现象确认 | 用户质疑"T13 评测结果反而下降了"？忠实度 56.0%（目标 100%）、Citation 40.7% | 双通道架构上线后核心指标不升反降——确认 Bug 真实存在，并非偶发波动 | 进入 T13 日志逐条分析层 | — |
| N2 | L1→L2 追问 | 为什么 `DecoupledPipeline` 在 T13 日志中出现 0 次？ | 搜索发现日志中完全不包含双通道标记——评测路径的双通道代码块从未被执行。对照 AgentDialog.cs 发现：评测路径 L624-645 仅做 OutputSanitizer，对话路径 L392-406 有完整的消毒+渲染+合并三件套 | 确认根因为「评测路径双通道不完整」 | — |
| N3 | 横向对比 | 对话路径 vs 评测路径——为什么一个完整一个残缺？ | 对话路径是初始实现（完整三件套），评测路径是后续追加的——追加时按最小策略只做了消毒验证效果，从未并排对照 | 补全评测路径 + 建立对称性检查机制 | 仅补全不建机制：下次新增路径仍会遗漏 |
| N4 | L2→L3 追问 | `evalToolCalls.Count == 0` 时整个双通道块被跳过——为什么没有 else？ | 原始设计假定「有工具调用 = 需要消毒，无工具调用 = LLM 不会编造 GB 编号」（被 Bug-013 推翻的假设）。实际上无工具调用时 LLM 更容易自由编造 | 新增 else 分支：消毒 + BuildNoResult | 不加 else：与 Bug-020 叠加后 info_query 13 条全部零保护 |
| N5 | 方案分支 | else 兜底：完全替换 LLM 输出 vs 消毒后合并 BuildNoResult？ | 完全替换会丢掉 LLM 的有用信息（如"苯的 CAS 号是 77-85-6"），但 BuildNoResult 说"无法给出确定结论"——两者矛盾 | 选「消毒后合并」：先 OutputSanitizer 剔除幻觉引用，再在前面拼上 BuildNoResult 声明——LLM 的信息保留但加免责声明 | 完全替换：过于激进，丢失 LLM 有用信息且用户会困惑为什么明明有答案却说不知道 |
| N6 | 实现决策 | 远程服务器运行的是旧白名单代码（3 个工具），本地已有 6 个——为什么没同步？ | 代码推送到 gitee 后自动 pull+rebuild 流程未触发——远程二进制仍是旧版本。P0 FIX 只在本地存在但未在远程生效 | 先推代码→远程 pull+build→重跑 T13 验证完整效果 | 直接改二进制：破坏部署可追溯性 |

---

## 思维链路（对话复盘）

> 记录本次 Bug 分析的关键思考节点，使后续复盘时不依赖记忆即可还原推理过程。

| # | 节点类型 | 触发问题 / AI 追问 | 关键发现 | 决策 | 否决的路径 |
|:---:|------|------|------|------|------|
| N1 | 现象确认 | 忠实度 56.6%、Citation 45.8%，日志只输出裸数字 | 只知道"低"，不知道"为什么低"——诊断信息丢失 | 确认需补全审计链路 | — |
| N2 | L1→L2 追问 | 评测方法内部有详细信息吗？为什么丢掉了？ | `ReflectionVerifier` 有每条声明的 chunks 结果，但 `ClaimVerification` 缺少 `SearchQuery`/`ChunksReturned` 字段，`EvalEngine` 也只取聚合数字 | 数据在传递链上逐层丢失 | — |
| N3 | 横向对比 | Faithfulness 和 Citation 的可追溯需求有何异同？ | Faithfulness 需 KB 检索链（query→chunks→match），Citation 需 chunk 匹配链（cited→source_chunk_id）——各自特有语义字段 | 分别设计 `ClaimDetail` 和 `CitationTrace`，不合并 | 合并会丢失各自特有字段（如 ClaimDetail 的 SearchQuery vs CitationTrace 的 CitedRegulation） |
| N4 | 方案分支 | 审计数据存 EvalResult vs 独立文件？ | EvalResult 是序列化到 `eval_report.json` 的单一载体——审计数据与评分数据应在同一文件以保持数据一致性 | 三个新字段加到 EvalResult | 独立文件：增加 I/O 开销，且破坏单一数据源原则 |


---

### Bug-024：结论提取器数据源误匹配 — 危险类别查询9条结论虚报⚠️ {#bug-024}

| 字段 | 内容 |
|------|------|
| **发现日期** | 2026-07-03 |
| **严重等级** | 🟡 P1 — 危险类别查询（A类）结论准确率虚低至40%，但LLM实际输出正确；误导性能评估方向 |
| **发现场景** | T13 全量回归（20260703_151433）日志审查 — A类15条用例中9条被标记⚠️结论不匹配（准确率仅40%=6/15），但逐条对照LLM输出文本发现所有9条的GB编号输出均正确。例如C003（液氯）：LLM输出"GB 30000.6、GB 30000.18"，eval却提取为"GB 30000.24"（日志行176 vs 184）——该编号既不出现在LLM输出也不在工具REGULATIONS中 |
| **影响模块** | `Agent1/Services/Eval/EvalEngine.cs`（结论提取逻辑，精确行号待定位）+ 评测集预期答案数据结构 |
| **根因** | **L1 现象**：A类15条中9条结论⚠️，结论准确率40%，提取的GB编号与LLM输出原文不一致；**L2 调用链**：结论评估流程中，结论提取步骤未从LLM最终输出文本中正则抓取GB编号——提取的数据源疑似指向工具REGULATIONS列表或评测集预期答案，而非LLM输出；**L3 设计冲突**：评测框架对CheckHazardCategory结论采用"提取第一个匹配的GB编号"策略，但多GB输出的情况（qwen3:8b输出2-5个编号）下"第一个"在不同数据源中含义不同——LLM输出中的第一个可能是GB 30000.6，而工具REGULATIONS中的第一个可能是GB 30000.5或GB 30000.24 |
| **修复** | 待修复。方案方向：① 明确结论提取数据源为LLM最终输出文本（`responseText`），而非工具结果或预期答案；② 增加交叉验证：提取的GB编号必须在LLM输出原文中存在，否则fallback到原文正则匹配；③ 多GB编号场景改为取LLM输出中第一个编号（而非工具列表第一个） |
| **修复提交** | — |
| **关联 Bug** | [Bug-021](#bug-021)（同为结论判定缺陷族——Bug-021是判定逻辑过松，Bug-024是提取数据源错误）；[Bug-022](#bug-022)（审计追踪使本Bug发现成为可能——逐条对照才发现提取与输出不一致） |
| **验证方法** | T13重跑后，A类15条中⚠️结论数应减少至≤3（允许LLM真实输出错误的情况），且每条⚠️的提取编号应能在LLM输出原文中找到对应 |
| **教训** | ① **数据源溯源是评测框架基本要求**：任何一个评测指标的计算，其输入数据必须能追溯到明确的数据源（是LLM原文还是工具结果还是预期答案），否则评测结果失真；② **多值场景的首条策略陷阱**：当数据源可能有多个值时（如多GB编号），"取第一个"的策略在不同数据源间不能简单替换——必须在设计文档中明确"第一个"的定义域 |
| **追问深度** | 3 层（L1 结论虚报⚠️ → L2 提取数据源非LLM输出 → L3 多GB编号下"首条策略"的数据源歧义） |

## 思维链路（对话复盘）

> 记录本次 Bug 分析的关键思考节点，使后续复盘时不依赖记忆即可还原推理过程。

| # | 节点类型 | 触发问题 / AI 追问 | 关键发现 | 决策 | 否决的路径 |
|:---:|------|------|------|------|------|
| N1 | 现象确认 | 为什么A类结论准确率只有40%（6/15）而工具触发率是100%？ | A类15条全部正确触发了CheckHazardCategory工具且参数正确，但9条结论被标记⚠️——矛盾点：工具都触发成功了为什么结论还会错？ | 逐条对照9条⚠️用例的LLM输出与eval提取结果 | — |
| N2 | L1→L2 追问 | 逐条对照发现：C003 LLM输出"GB 30000.6"但eval提取"GB 30000.24"——GB 30000.24从哪里来？ | 搜索整个日志行176（LLM输出）、行178（工具REGULATIONS），均不包含GB 30000.24。该编号既不是LLM生成的也不是工具返回的——推测来自评测集预期答案或结论提取逻辑中的硬编码映射 | 确认根因为"结论提取数据源错误"——未从LLM输出提取 | 怀疑LLM幻觉：但输出中根本不包含该编号=不是幻觉 |
| N3 | 横向对比 | 6条✅的用例有什么共同特征？ | C001/C002/C006/C008/C011/C015全部通过——它们提取的编号均同时出现在LLM输出和工具REGULATIONS中（如GB 30000.7）。而9条⚠️的编号在LLM输出中找不到——说明通过/失败的判定取决于提取逻辑是否"恰好"命中了LLM输出中的编号 | 修复方向：强制从LLM输出提取并交叉验证 | 直接改预期答案：治标不治本，下次LLM输出变化又会错 |
| N4 | 方案分支 | 修复方式：改结论提取数据源 vs 增加提取-原文交叉验证？ | 仅改数据源不够——即使从LLM输出提取，多GB编号场景下仍可能取到非预期编号 | 选"数据源修复 + 交叉验证"双层策略：先从LLM输出提取，再验证提取结果在原文中存在 | 仅交叉验证：如果数据源本身是错的，验证只能报告不一致但不能修复 |

---

### Bug-025：Answer Relevance 评估器 FC Required() 导致重复输出+流式截断 {#bug-025}

| 字段 | 内容 |
|------|------|
| **发现日期** | 2026-07-03 |
| **严重等级** | 🟡 P1 — 单case产生66+条重复日志+LLM输出被5000字符截断，信息丢失且日志膨胀 |
| **发现场景** | T13 全量回归（20260703_151433）I001用例（法规版本查询）— Answer Relevance评分环节产生66+条完全相同的评分解释文本（"AI回答直接回应了用户关于知识库收录情况的询问…"），随后触发流式输出截断「⚠️ [截断] 输出超过5000字符上限，停止流式接收」（日志行2146） |
| **影响模块** | `Agent1/Services/Eval/EvalEngine.cs`（Answer Relevance评估LLM调用）+ 流式输出截断逻辑（5000字符上限） |
| **根因** | **L1 现象**：I001用例Answer Relevance评分重复输出66+次，LLM输出被截断；**L2 调用链**：Answer Relevance评估使用独立LLM调用（qwen3:8b），其FC策略为`RequiredFunctionChoiceBehavior`（日志行2071 `FC=RequiredFunctionChoiceBehavior`）——由于该评估prompt不涉及业务工具调用，Required()强制调用但无工具可用 → LLM进入重复生成模式（与Bug-018/Bug-019同族的FC配置错误）；同时流式输出的5000字符截断保护生效，中断了无限重复；**L3 设计冲突**：评测框架中的"评分LLM调用"普遍使用Required()策略（继承自业务评测的FC配置），但评分LLM的prompt不包含工具调用意图——Required()在此场景下是错误配置 |
| **修复** | 待修复。方案方向：① Answer Relevance（及所有评分类LLM调用）改用`FunctionChoiceBehavior.None()`或`Auto()`，禁止Required()；② 5000字符截断应记录为WARNING而非静默截断——当前截断后丢失的LLM内容可能包含有用信息；③ 增加重复输出检测：连续3条完全相同的输出触发early-stop |
| **修复提交** | — |
| **关联 Bug** | [Bug-018](#bug-018) [Bug-019](#bug-019)（同属FC Required()误用族——Bug-018是业务评测死循环，Bug-019是HyDE递归，Bug-025是评分LLM重复输出）；[W1](#系统弱点了然表)（LLM流式输出失控，截断=500字符标记） |
| **验证方法** | T13重跑后，I001用例Answer Relevance评分日志行数应≤5条（而非66+条），且不应出现`⚠️ [截断]`标记 |
| **教训** | ① **FC策略不能盲目继承**：评分LLM调用与业务LLM调用的FC需求不同——评分不需要工具，应显式设为None()而非沿用Required()；② **截断保护需要可见性**：当前5000字符截断是静默保护，应在截断处输出截断前行数和丢失字节数，方便诊断 |
| **追问深度** | 2 层（L1 重复输出+截断 → L2 FC Required()误用于评分LLM → 同族Bug-018/019已论证的FC策略继承问题） |

## 思维链路（对话复盘）

> 记录本次 Bug 分析的关键思考节点，使后续复盘时不依赖记忆即可还原推理过程。

| # | 节点类型 | 触发问题 / AI 追问 | 关键发现 | 决策 | 否决的路径 |
|:---:|------|------|------|------|------|
| N1 | 现象确认 | I001日志区出现66+条完全相同的评分文本——是输出bug还是日志bug？ | 日志行2072-2145全部是Answer Relevance评分解释文本，内容完全一致（逐字相同）——排除日志重复写入，确认是LLM输出重复 | 标记为Bug-025：FC Required()误用导致评分LLM重复输出 | — |
| N2 | 横向对比 | 与Bug-018/Bug-019有何异同？ | 同为FC Required()误用模式：Bug-018是业务评测→工具循环，Bug-019是HyDE→工具递归，Bug-025是评分LLM→无工具可用→重复生成。三者根因相同（Required()在不该用的场景被继承），表现不同（循环/递归/重复） | 将Bug-025归入FC Required()误用族 | 独立归类：丢失与Bug-018/019的共性，无法形成系统性修复策略 |
| N3 | 方案分支 | 修复：仅改Answer Relevance的FC策略 vs 全局审查所有评分LLM的FC配置？ | 仅改一个点=下次其他评分LLM还会出同类问题 | 选全局审查：所有评分类LLM调用统一检查FC配置，Required()→None() | 仅改Answer Relevance：下次Citation/Faithfulness评分LLM也会遇到同样问题 |

---

## Bug 分类统计

| 类别 | Bug 数 | 典型表现 |
|------|:---:|------|
| LLM 流式输出质量 | 2 | 死循环、重复输出 |
| LLM 幻觉 | 1 | GB 编号错误 |
| LLM 配置 | 5 | FC 退化、重试死亡螺旋、Required() 死循环、HyDE+FC 递归、评分LLM重复输出 |
| 编码/乱码 | 1 | UTF-8 → 锟斤拷 |
| 数据库事务 | 2 | tx 重复 / 未提交 |
| 并发安全 | 2 | Dictionary / async void |
| 检索算法 | 2 | RRF 去重 / BM25 除零 |
| 工具函数 | 1 | 正则表达式失效 |
| 评测系统 | 7 | 格式匹配 / 评分归零 / 白名单不足 / 结论判定过松 / 无审计追踪 / 双通道不完整 / 结论提取数据源错误 |
| 异常处理 | 1 | 检索异常错误标记 |
| C# 语法 | 1 | 字符串插值格式说明符歧义 |
| **合计** | **25** | |

---

## 思维链路记录（为什么要有这个？）

> 🔴 **核心问题**：传统 Bug 记录只保留"结果"（根因是什么、怎么修的），丢失了"过程"（怎么一步步想到的）。下次复盘时，你只看到结论，看不到推理——相当于每次都从头推演，无法站在上次的肩膀上。
>
> **解决方案**：每个 Bug 增加「思维链路」字段，记录关键思考节点、分支决策、否决过的路径。使复盘时不依赖记忆即可还原完整推理过程。

**思维链路格式**：

```markdown
## 思维链路（对话复盘）

> 记录本次 Bug 分析的关键思考节点，使后续复盘时不依赖记忆即可还原推理过程。

| # | 节点类型 | 触发问题 / AI 追问 | 关键发现 | 决策 | 否决的路径 |
|:---:|------|------|------|------|------|
| N1 | 现象确认 | 用户发现 XX 日志异常 | 确认 Bug 表现范围 | 进入 L1 分析层 | — |
| N2 | L1→L2 追问 | 为什么 XX 会导致 YY？ | 调用链追踪到 ZZ | 深入原理层而非修症状 | 停在这个 Bug 修症状就完了 |
| N3 | 横向对比 | 和 Bug-NN 有什么异同？ | 同族不同触发条件 | 对比分析找共性 | — |
| N4 | 方案分支 | 方案 A 还是方案 B？ | A 向后兼容、B 简单但风险大 | 选 A | B：硬编码改 None，影响 Reflection 路径 |
| N5 | 实现决策 | 某路径用 Auto 还是 None？ | 手动 ParseToolCalls 不依赖 SK FC | 分层策略 | 全部统一：丢失场景差异 |
```

**节点类型分类**：

| 类型 | 含义 | 示例 |
|------|------|------|
| 现象确认 | 从日志/监控中确认 Bug 真实存在 | "t13_heartbeat.log 中 F005 卡死在 1800+ 行循环" |
| L1→L2 追问 | 从表面现象向下追问到调用链机制 | "为什么 HyDE 提示词会触发 CheckStorageCompatibility？" |
| L2→L3 追问 | 从调用链追问到设计冲突/历史演进 | "为什么 InvokeStreamAsync 硬编码 Required()？Git 历史上谁改的？" |
| 横向对比 | 与同类 Bug 对比找共性和差异 | "Bug-018 vs Bug-019：都是 FC 配置问题，为什么表现不同？" |
| 方案分支 | 在多个方案之间做选择 | "参数化 vs 硬编码改 None：哪个更安全？" |
| 实现决策 | 具体实现层面的选择 | "Reflection Step1 用 Auto 还是 None？" |

> 💡 **一句话记忆**：Bug 记录的价值不在于"这次修好了什么"，而在于"下次能多快定位到同类问题"。思维链路是下次定位的加速器。

---

### Bug-026：评测集7条危险类别 expected_regulation_number 与 ChemicalSubstanceDatabase 不对齐 {#bug-026}

| 字段 | 内容 |
|------|------|
| **发现日期** | 2026-07-03 |
| **严重等级** | 🔴 P0 — 15条危险类别查询中7条结论被误判为幻觉 |
| **发现场景** | T13 合规评测集日志深度分析 — C003/C004/C005/C007/C009/C010/C013 共 7 条触发 `Level4 幻觉扣除`，但 LLM 输出的 GB 编号实际来自 ChemicalSubstanceDatabase 工具调用结果，属于正确引用 |
| **影响模块** | `Data/ComplianceEvalSet.json` C003-C013, `Agent1/Models/EvalModels.cs` (新增 StringOrListConverter), `Agent1/Services/Eval/EvalEngine.cs` CheckConclusion/L4校验 |
| **根因** | **评测集编写者手动填写的 GB 编号与 ChemicalSubstanceDatabase 中实际映射不一致**。两个根本性错误：① C003 液氯的 `GB 30000.24` 是"氧化性液体"标准号，但氯是气体，数据库映射为 GB 30000.6（加压气体）；② C004/C009 使用 GB 30000.14（氧化性液体），但硝酸铵和高锰酸钾均为固体，数据库映射为 GB 30000.15（氧化性固体）。评测集的 GB 编号编号体系（.5=易燃气体, .14=氧化性液体, .24=加压气体）与数据库编号体系（.3=易燃气体, .15=氧化性固体, .6=加压气体）之间存在系统性偏移 |
| **修复** | ① 修正 7 条评测数据的 `expected_regulation_number` 为数组格式 `["GB 30000.6","GB 30000.5","GB 30000.18"]`，对齐数据库主危险类别；② 新增 `StringOrListConverter` JSON 转换器，兼容字符串和数组两种格式；③ EvalEngine.CheckConclusion 改为列表匹配（任一预期编号出现在回答中即通过）；④ EvalEngine Level4 校验改为多预期编号交叉比对 |
| **修复提交** | 003f2854 |
| **关联 Bug** | [Bug-024](#bug-024) 结论提取器数据源误匹配（同一评测集的数据质量问题）；[Bug-027](#bug-027) Citation Accuracy 误判（同为评估框架与数据源架构假设不匹配） |
| **验证方法** | 重跑 T13 评测集危险类别段（C001-C015），预期结论准确率从 40%（6/15）恢复到 >90% |
| **教训** | **评测集是代码的一部分** — 评测数据中引用的领域实体（GB 编号、化学品名、法规版本）必须与系统数据源严格同步，不得人工手工维护。建议增加 CI 步骤：评测集加载时自动逐条校验 `expected_regulation_number` 是否存在于 `ChemicalSubstanceDatabase` 中 |
| **追问深度** | 4 层（现象：Level4 幻觉扣除 → 代码：CheckConclusion 单字符串匹配 → 数据库：ChemicalSubstanceDatabase 实际 GB 映射 → 评测集：人工填写的数据源错误） |

### 思维链路（Bug-026）

| # | 节点类型 | 触发问题 / AI 追问 | 关键发现 | 决策 | 否决的路径 |
|:---:|------|------|------|------|------|
| N1 | 现象确认 | T13 日志 C003 液氯 `Level4 幻觉扣除: 回答含7个GB编号但无预期 GB 30000.24` | 液氯是气体，GB 30000.24 是"氧化性液体"标准号 — 预期值本身可疑 | 进入数据库交叉验证 | — |
| N2 | L1→L2 追问 | ChemicalSubstanceDatabase 中液氯的实际 GB 映射是什么？ | 数据库返回 6 个 GB 标准号（30000.6/5/18/19/20/28），无一个匹配 .24 | 确认评测集数据错误，非 LLM 幻觉 | 怀疑 LLM 遗漏了正确的 GB 编号 |
| N3 | 横向对比 | C004/C009 也是 GB 30000.14 被拒 — 同样的模式？ | 硝酸铵和高锰酸钾都是固体，数据库用 GB 30000.15（氧化性固体），评测集用 .14（氧化性液体） | 识别编号体系偏移模式 | — |
| N4 | 方案分支 | 改评测集单值 → 数组 vs 只改评测集不改代码 | 单值只能选一个主危险类别，丢失其他合法引用；数组格式更真实反映多危险类别场景 | 选数组格式 + StringOrListConverter | 只修评测集不改代码：下次加评测数据还会出错 |

---

### Bug-027：Citation Accuracy 误判数据库源 GB 编号为幻觉 {#bug-027}

| 字段 | 内容 |
|------|------|
| **发现日期** | 2026-07-03 |
| **严重等级** | 🔴 P0 — Citation Accuracy 系统性虚低（42 条疑似幻觉中仅 8-12 条为真实幻觉） |
| **发现场景** | T13 日志分析 — `CheckRegulationVersion` 返回的 GB 编号（如 GB 15603、GB 18218）在知识库 KB 校验时被标记为"疑似幻觉"，因为 KB 中检索不到这些编号的文档全文 |
| **影响模块** | `Agent1/Services/Eval/EvalEngine.cs` L254-L268 (Level4 KB 验证循环) |
| **根因** | **架构假设与实现不一致**。Citation Accuracy 的 KB 校验建立在"所有 GB 编号都来自 RAG 文档"的假设上，但 `CheckRegulationVersion` 返回的 GB 编号来自 `ChemicalSubstanceDatabase` 硬编码字典（8 条法规版本记录），非 RAG 文档。用户问"GB 15603 最新版本"时 → FC 调用 CheckRegulationVersion → 数据库返回"现行 2022 版" → LLM 回答包含 GB 15603 → KB 检索不到 → 误判为幻觉。42 条"疑似幻觉"中，法规版本查询（I 类）、重大危险源（H 类）等数据库源数据约占 30-34 条 |
| **修复** | 在 Level4 KB 验证循环中插入 `ChemicalSubstanceDatabase.GetRegulationVersion(reg)` 查询：若 DB 中有此法规记录 → 输出 `⚡ 数据库源（非RAG），跳过幻觉检查` → `continue` 跳过 RAG 检索。仅当 DB 也查不到时 → 继续原有 RAG 检索逻辑 |
| **修复提交** | 003f2854 |
| **关联 Bug** | [Bug-026](#bug-026) 评测集数据错误（同为数据源假设不匹配） |
| **验证方法** | 重跑 T13 评测集，预期：① 法规版本查询类 Citation Accuracy 不再为 0%；② `⚡ 数据库源` 日志出现次数 ≈ I 类 + H 类涉及 GB 编号数量；③ 真实幻觉计数从 42 降至 8-12 |
| **教训** | **数据源感知的验证逻辑** — 任何验证步骤（Citation Accuracy、忠实度、幻觉检测）必须知道所验证的数据来自哪个数据源（RAG 文档 / SQLite / C# 字典 / API），不可假设统一来源。建议在 ExtractedFacts 中增加 `DataSource` 字段标注每条法规引用的来源 |
| **追问深度** | 3 层（现象：Citation=0% → 代码：KB 检索验证逻辑 → 架构：CheckRegulationVersion 数据源为 C# 字典而非 RAG） |

### 思维链路（Bug-027）

| # | 节点类型 | 触发问题 / AI 追问 | 关键发现 | 决策 | 否决的路径 |
|:---:|------|------|------|------|------|
| N1 | 现象确认 | T13 Citation Accuracy 系统性偏低，大量 `🔍 Level4 KB验证: ✗` | 被标记为幻觉的编号（GB 15603/18218/30871）恰好是法规版本查询涉及的编号 | 怀疑 Citation Accuracy 校验逻辑有缺陷 | — |
| N2 | L1→L2 追问 | KB 检索不到这些 GB 编号 → 编号从哪里来的？ | `CheckRegulationVersion` → `ChemicalSubstanceDatabase.GetRegulationVersion()` → C# 硬编码字典 `_regulationVersions` | 确认数据源不匹配 | 怀疑是 RAG 文档缺失 → 被否决：这些编号确实不在文档中 |
| N3 | 方案分支 | 修改 Citation Accuracy 校验 → 区分数据源 vs 给数据库源编号也建文档索引 | 建索引是治标：下次新增数据库源还需要同步维护文档 | 选区分数据源方案：校验前先查 DB | 建 RAG 文档索引：维护成本高且违反"DB 即权威源"设计原则 |

---

### Bug-028：Qwen3:8b 自行编造法规年代版本号 + 三层约束修复 {#bug-028}

| 字段 | 内容 |
|------|------|
| **发现日期** | 2026-07-03 |
| **严重等级** | 🟡 P1 — LLM 编造版本年份导致合规结论不可信 |
| **发现场景** | T13 日志 — 法规版本查询 I001：用户问"GB 15603 最新版本"，LLM 回答 `GB 15603-2023`，但 ChemicalSubstanceDatabase 记录的实际版本为"现行 2022 版" |
| **影响模块** | `Agent1/Services/Compliance/PromptSanitizer.cs` SanitizeSystemPrompt, `Agent1/Services/Compliance/OutputSanitizer.cs` ValidateVersionYear + GbWithYearRegex |
| **根因** | Qwen3:8b 在无工具调用时，基于训练数据中的分布记忆自行推断版本年份。LLM 训练数据中可能包含旧版讨论或错误引用，导致编造出 -2023（不存在）等版本号。根本原因是**版本号生成路径未经过确定性数据源** |
| **修复** | **三层约束防御**：① **L1 Prompt 约束**：`PromptSanitizer.SanitizeSystemPrompt()` 注入指令「禁止在编号后追加年代版本号，版本号由系统从数据库查询」；② **L2 输出校验**：`OutputSanitizer.ValidateVersionYear()` — 正则 `GB\s*/?T?\s*\d{4,5}-\d{4}` 提取版本号 → 查询 `ChemicalSubstanceDatabase.GetRegulationVersion()` → 年份不匹配则强制替换为正确版本；③ **L3 硬兜底**：`OutputSanitizer.Sanitize()` 末尾调用 L2，确保所有路径都经过版本校验 |
| **修复提交** | 003f2854 |
| **关联 Bug** | [Bug-013](#bug-013) Qwen3:8b GB 标准编号幻觉（同为 LLM 编造 GB 相关数据） |
| **验证方法** | 重跑 T13 I 类（法规版本查询），预期：① 不再出现 -2023 等错误年份；② 控制台输出 `🔧 版本校正` 确认替换生效；③ 若 LLM 输出 `GB 15603-2022`（正确年份），不触发替换 |
| **教训** | **任何有时效性的数值（版本年份、截止日期、价格）必须由确定性代码注入，不可依赖 LLM 记忆**。LLM 的训练数据截止日期与当前时点之间存在知识断层，是版本幻觉的天然土壤。Prompt 约束是软防御，OutputSanitizer 正则+DB 校验是硬兜底，两者缺一不可 |
| **追问深度** | 3 层（现象：GB 15603-2023 错误年份 → 模型：Qwen3 训练数据时间差 → 架构：版本号生成路径未经过确定性数据源） |

### 思维链路（Bug-028）

| # | 节点类型 | 触发问题 / AI 追问 | 关键发现 | 决策 | 否决的路径 |
|:---:|------|------|------|------|------|
| N1 | 现象确认 | I001 回答 `GB 15603-2023` 但数据库记录为 2022 | 不是 T13 系统级问题，是 LLM 输出级幻觉 | 定位为 LLM 幻觉而非评测框架错误 | — |
| N2 | L1→L2 追问 | 为什么 LLM 会知道 2023？模型训练数据中有这个年份吗？ | Qwen3-8B 训练数据截止 2024 年中，可能包含网络讨论中错误引用的"GB 15603-2023" | 确认根因是训练数据污染，非系统 Bug | 怀疑数据库版本记录过时 → 查证国标官网确认 2022 为最新版 |
| N3 | 方案分支 | Prompt 约束（"禁止输出年份"）vs 代码硬校验（正则+DB替换） vs 两者都要 | 只做 Prompt 约束无法保证 100% 生效（LLM 可能忽略指令）；只做代码校验对用户不友好（悄悄替换但不告知） | 三层防御：Prompt 约束 → 正则提取 → DB 校验替换 | 单层方案：化工安全系统要求 0 失误，必须冗余防御 |

---

## 录入模板（新 Bug 使用）

```markdown
### Bug-XXX：{标题} {#bug-xxx}

| 字段 | 内容 |
|------|------|
| **发现日期** | YYYY-MM-DD |
| **严重等级** | 🔴 P0 / 🟡 P1 / 🔵 P2 |
| **发现场景** | 如何发现的（测试用例 / 代码扫描 / 用户反馈） |
| **影响模块** | 涉及的文件（精确到行号） |
| **根因** | 为什么发生（代码逻辑/环境/模型行为） |
| **修复** | 怎么修的（方案 + 关键代码变更） |
| **修复提交** | commit hash |
| **关联 Bug** | 相关 Bug ID |
| **验证方法** | 如何确认已修复 |
| **教训** | 沉淀的工程原则 |
| **追问深度** | 追问了几层、各层触达了什么（L1现象→L2原理→L3设计冲突→...） |
| **思维链路** | 见上方「思维链路记录」格式，用表格记录关键思考节点和分支决策 |
```
